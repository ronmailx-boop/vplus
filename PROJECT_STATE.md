# PROJECT_STATE — vplus

## סטטוס כללי
פרויקט חדש, בבנייה מאפס. הבנייה מתבססת על הריפו הקיים `ronmailx-boop/shopping-list` (פרטי), שצורף לסשן ושוכפל מקומית ל-`/workspace/shopping-list` לצורך עיון.

**החלטה ארכיטקטונית (עודכן שוב בשלב 45):** האפליקציה עדיין **ללא Firebase** — אין Firestore, אין Firebase Auth, אין Cloud Functions. **אבל** בשלב 45 נוסף **Supabase** כ-backend ייעודי **רק** לפיצ'ר שיתוף רשימה חי בין כמה משתמשים (ראה שלב 45) — סטייה מודעת ומוסברת מההחלטה המקורית "ללא שרת", לבקשת המשתמש. שאר האפליקציה (רשימות רגילות, לא-משותפות) ממשיכה לעבוד לגמרי מקומית (localStorage), בלי שום תלות ברשת.

## החלטות היקף (Out of Scope כרגע)
- ללא Firebase (ראה מעלה — Supabase נוסף בשלב 45 לפיצ'ר שיתוף חי בלבד, לא Firebase ולא backend כללי לכל האפליקציה).
- **ללא כפתור/מודול "פיננסי"** — לא ייבנה חיבור לחשבונות בנק או כרטיסי אשראי ולא שליפת נתונים משם (בניגוד ל-`scripts/bank-sync` שקיים ב-shopping-list). אין להעתיק את הפיצ'ר הזה מהריפו המקורי.
- **ללא שורת כפתורי הפעולות מ-shopping-list**: מיון, תרגום, שיתוף, סיווג מחדש, מדריך, תבנית — כל שורת הכפתורים הזו לא תיכלל ב-vplus.
- **ללא כפתור "הגדרות" וכל מה שבתוכו** — לא ייכלל ב-vplus, כולל כל תת-המסכים/אפשרויות שהיו מתחתיו ב-shopping-list.
- **ללא התראות Push** — לא ייכלל מנגנון push notifications (בניגוד ל-`push-notifications.js` ו-`notification-handler.js` שקיימים ב-shopping-list).
- **ללא התחברות עם חשבון Google** — לא ייכלל Google Sign-In / OAuth (בניגוד ל-`login.html` שקיים ב-shopping-list).

## תובנות ראשוניות מ-Shopping-list (מקור ההשראה)
- אפליקציית PWA וניל-JS (ללא build step, אין `package.json` בשורש) עם Firebase Web SDK בצד לקוח.
- מבנה עיקרי: `index.html`, `script.js`, `style.css`, `sw.js`, `manifest.json` — קבצים גדולים מאוד (500K+), כנראה לא מפוצלים למודולים בפועל בשורש.
- יש גם תיקיית `js/` עם פיצול חלקי: `js/core` (constants, firebase-config, store, utils), `js/features` (import-export, notifications), `js/ui` (charts, lists, modals, wizard) — כנראה קוד ישן/חדש יותר או ניסיון מודולריזציה.
- `functions/` — Firebase Cloud Functions (יש לו `package.json` נפרד).
- `firestore.rules`, `.firebaserc`, `firebase.json` — תשתית Firebase Hosting + Firestore.
- יש התחלת תמיכה ב-Push Notifications (`push-notifications.js`, `notification-handler.js`) ו-PWA (ikonים, manifest, `.well-known/assetlinks.json` ל-TWA).

## מבנה התיקיות שנוצר (שלד ראשוני)
```
vplus/
├── index.html          # דף כניסה, RTL, טוען js/app.js כמודול
├── style.css
├── manifest.json       # PWA manifest (עברית, RTL)
├── sw.js               # Service Worker בסיסי (install/activate)
├── .gitignore
├── js/
│   ├── app.js          # נקודת כניסה
│   ├── core/
│   │   ├── constants.js
│   │   ├── store.js
│   │   └── utils.js     # כולל sanitize() למניעת XSS
│   ├── features/        # ריק, ל-features עתידיים
│   └── ui/               # ריק, לרכיבי UI עתידיים
├── assets/icons/         # ריק, לאייקוני PWA עתידיים
└── docs/legal/           # ריק, למסמכי משפט (privacy-policy וכו' לפי CLAUDE.md)
```
אין `functions/` (Firebase Cloud Functions) ואין תלות ב-Firebase כלל בשלב זה.

## תוכנית בנייה מלאה
המשתמש אישר בנייה מלאה של אפליקציית רשימת קניות, זהה גרפית ותפעולית ל-shopping-list (חוץ מההחרגות למעלה), עם מיזוג ל-main בסיום. תוכנית מפורטת (10 שלבים) נשמרה גם ב-`/root/.claude/plans/frolicking-dreaming-bonbon.md`. הוחלט גם: בלי פאנל התראות פנימי/תזכורות תאריך יעד, בלי סריקת קבלה (OCR).

### התקדמות
- [x] שלב 1: Data layer — `js/core/constants.js` (CATEGORIES, CATEGORY_KEYWORDS ללא פיננסי, detectCategory), `js/core/store.js` (db schema, load/save ל-localStorage, CRUD רשימות/פריטים, history, stats). נבדק ידנית ב-Node.
- [x] שלבים 2-4: מעטפת UI ראשית + CRUD + מודלים — `index.html`/`style.css` (פלטה מ-shopping-list), `js/ui/render.js` (Lists/Summary/Stats + קיבוץ קטגוריות), `js/ui/modals.js` (מודלים גנריים + toast/undo), `js/features/item-crud.js`, `js/features/list-crud.js`. **נבדק end-to-end ב-Playwright**: הוספה/עריכה/מחיקה+undo/toggle של פריט, יצירה/מחיקה/מעבר בין רשימות, טאבים Lists/Summary/Stats, שמירה ב-localStorage בין רענוני דף — הכל עובד בלי שגיאות קונסולה.
- [x] שלב 5: היסטוריה וסטטיסטיקה — `js/features/history.js` (סיום רשימה→היסטוריה, מודל היסטוריה, שחזור/מחיקה), `js/features/stats.js` (גרפים חודשי/קטגוריה/פריטים פופולריים עם Chart.js דרך CDN). נבדק ב-Playwright: סיום רשימה, מודל היסטוריה, שחזור עובדים תקין. **הערה**: Chart.js נטען מ-CDN חיצוני (jsdelivr) — בסביבת הבדיקה הנוכחית (sandbox) ה-CDN חסום ע"י מדיניות הפרוקסי, כך שלא ניתן היה לאמת ויזואלית את הגרפים כאן; הקוד מוגן (`if (window.Chart)`) כך שהאפליקציה לא קורסת בלעדיו. אצל משתמשי קצה אמיתיים (דפדפן רגיל) ה-CDN אמור לעבוד כרגיל.
- [x] שלב 6: Autocomplete/מחירון/חיפוש — `js/features/autocomplete.js` (הצעות אוטומטיות בעת הוספת פריט לפי היסטוריית מחירים, מודל מחירון עם חיפוש/עריכת מחיר/הסתרה), חיפוש בין רשימות ב-Summary. נבדק ב-Playwright — עובד תקין, ללא שגיאות.
- [x] שלב 7: ייבוא — `js/features/import.js` (הדבקת טקסט עם פרסור שם/מחיר/כמות, ייבוא מהלוח, ייבוא מ-Excel דרך SheetJS/CDN), `js/features/voice.js` (הוספת פריט בקול, Web Speech API). נבדק ב-Playwright (טקסט+לוח עובדים; SpeechRecognition זמין בדפדפן, לחיצה לא קורסת). ה-XLSX וה-Chart.js נטענים מ-CDN חיצוני, חסום בסביבת ה-sandbox אך אמור לעבוד למשתמשי קצה.
- [x] שלב 8: גרירה-לסידור (SortableJS/CDN, לרשימות ולפריטים במצב עריכה), נעילת רשימה, בחירה/מחיקה מרובה (פריטים ורשימות), הדפסה — `js/core/store.js` (reorderListsOrder, reorderListItems, toggleListLock), `js/features/organize.js`. נבדק ב-Playwright: נעילה חוסמת עריכה, מצב עריכה+בחירה מרובה+מחיקה עובד לפריטים ולרשימות, הדפסה קוראת ל-window.print. **נשמט** מהתוכנית המקורית (לא קריטי, ניתן להוסיף בהמשך אם יתבקש): מצב קומפקטי (compact card height toggle) וחיוב חודשי קבוע/שורת "נדחה לחודש הבא" — פיצ'רים משניים מהמקור.
- [x] שלב 9: ליטוש PWA — נוצרו אייקונים אמיתיים (72-512px, "V" ירוק על רקע גרדיאנט סגול, תואם לוגו האפליקציה) ב-`assets/icons/`, `manifest.json` מלא (כל הגדלים, maskable, shortcuts ל"רשימה חדשה"/"סטטיסטיקות"), `sw.js` מלא (precache + network-first עם fallback ל-cache, בלי שום push handler), רישום ה-Service Worker ב-`app.js` עם טיפול שגיאות. נבדק ב-Playwright: SW נרשם ומופעל, אייקונים נטענים (0 שגיאות 404), קיצורי הדרך מה-manifest עובדים.
- [x] שלב 10: QA מקצה לקצה — הרצת תרחיש מלא ב-Playwright (הוספת פריטים בכמה קטגוריות, קיבוץ, סימון, עריכה, חיפוש, רשימה נוספת, מעבר בין רשימות, סיום רשימה→היסטוריה, סטטיסטיקה, שמירה בין רענונים) — **כל הבדיקות עברו, 0 שגיאות קונסולה אמיתיות**. צילום מסך ויזואלי אושר (RTL תקין, פלטת צבעים, mobile layout). מוזג ל-`main`.

## שלב 11: עיצוב מחדש "Nocturne" מ-Claude Design
המשתמש עיצב מחדש את הממשק ב-Claude Design (ערכת נושא כהה, ניווט עליון, אייקוני Phosphor, פונט Assistant) והעביר חבילת מסירה (ZIP, ראה `MIGRATION.md` בחבילה). הוחלפו 4 קבצים במלואם — `index.html`, `style.css`, `js/ui/render.js`, `js/features/stats.js` — בלי לגעת בלוגיקה/store/features אחרים (כל ה-id-ים וה-exports נשמרו תואמים). עודכן גם `manifest.json` (theme/background color ל-`#161826`) ו-`sw.js` (CACHE_NAME הועלה ל-v2 כדי לנקות cache ישן של style.css).

נבדק מחדש ב-Playwright את כל התרחישים (הוספה/עריכה/מחיקה+undo, קיבוץ קטגוריות, חיפוש, רשימות מרובות, סיום רשימה→היסטוריה, סטטיסטיקה, נעילה, מצב עריכה+בחירה מרובה, הדפסה, autocomplete/מחירון, ייבוא טקסט/לוח, קול) — **הכל עובד תקין**. שגיאות הקונסולה היחידות הן חסימת CDN חיצוניים (Chart.js, XLSX, SortableJS, Phosphor Icons, Google Fonts) על ידי מדיניות הפרוקסי בסביבת ה-sandbox — לא רלוונטי למשתמשי קצה אמיתיים. מוזג ל-`main`.

## שלב 12: וריאציית "כרטיסים" למסך הרשימה
דרך סקיל `/design` נבנו 3 כיווני עיצוב חלופיים למסך ה-Lists (כרטיסים / פנקס צפוף / נקודת קטגוריה) על canvas נפרד לצורך השוואה — לא נגעו בקוד. המשתמש בחר ב"כרטיסים" וביקש לשלב בפועל. שולב ב-`style.css` בלבד (שינוי CSS טהור, בלי לגעת ב-HTML/JS): `.items-container` קיבל `display:flex; flex-direction:column; gap:10px`, `.item-card` הפך מכרטיס-שורה עם קו מפריד לכרטיס מוגבה (רקע `--color-surface`, `border-radius:var(--radius-lg)`, `box-shadow:var(--shadow-sm)`), `.item-card.checked` מקבל רקע `--color-neutral-900` במקום opacity, `.category-header` קיבל padding מותאם לרווח החדש בין הכרטיסים. מצב עריכה/גרירה (`js/ui/render.js`) משתמש באותו class `item-card` אז קיבל את העיצוב אוטומטית בלי שינוי JS.

נבדק ב-Playwright מחדש: כל תרחישי ה-QA המלאים + נעילה/מצב עריכה/בחירה מרובה/הדפסה — **הכל עובד**. מוזג ל-`main`.

## שלב 13: סרגל הטאבים+FAB עבר לתחתית המסך
`nav.tab-bar` (טאבים רשימה/רשימות/תקציב + כפתור "+ פריט") הועבר ב-`index.html` מהמיקום ליד הראש (מתחת ל-list-name-bar) לסוף התוכן בתוך `#app` (אחרי שלושת ה-`<main class="page">`, לפני `#toastBar`) — כ-flex child אחרון עם `flex:none`, כך שהוא יושב אוטומטית בתחתית המסך (ה-`.page` הפעיל עם `flex:1` תופס את שאר הגובה). בתוך ה-nav שונה הסדר כך ש-`fabAddItem` ("+ פריט") ראשון (הכי מימין ב-RTL), ואחריו שלושת הטאבים. ב-`style.css`: `.tab-bar` — הוסר margin-top, `border-bottom` הוחלף ב-`border-top`, נוסף padding עם `env(safe-area-inset-bottom)`; `.fab-btn` — כיוון ה-auto-margin התהפך (`margin:0 0 6px auto` במקום `0 auto 6px 0`) כדי לשמר את הרווח החזותי בין הכפתור לאשכול הטאבים, הפוך כיוון.

נבדק ב-Playwright: הסרגל יושב נכון בתחתית, "+ פריט" ראשון/מימין, הטוסט לא מתנגש איתו ויזואלית, וכל תרחישי ה-QA המלאים + נעילה/מצב עריכה עדיין עובדים. מוזג ל-`main`.

## שלב 14: חזרה לעיצוב המקורי הבהיר/הצבעוני (החלפת Nocturne)
המשתמש ביקש לשחזר בקוד את העיצוב המקורי של shopping-list (סגול `#7367f0`/`#9055ff`, כרטיסים לבנים, סרגל תחתון בגרדיאנט) — לאחר שאושר על canvas נפרד (`/design`, artboards "הרשימה שלי"+"הוספת מוצר"). זהו שינוי טוקנים+קומפוננטות מלא, לא שינוי פיצ'רים:
- `style.css`: `:root` הוחלף במלואו לפלטה הבהירה (רקע/משטח/טקסט/אקסנט/ניוטרלים/danger — ראה מיפוי מפורט בתוכנית התכנון), `html,body` מקבל את הגרדיאנט הבהיר המקורי, `.btn-primary`/`.btn-danger` הפכו למלאים (fill) במקום מתאר (outline), `.tab-bar` הפך לסרגל גרדיאנט סגול מעוגל (`border-radius:35px 35px 0 0`), `.tab-btn.active` מקבל רקע-פיל לבן, `.fab-btn` הפך לעיגול ירוק גדול, `.item-card` קיבל `border-radius:25px` + מסגרת צבעונית לפי קטגוריה (`--cat`), נוספו `.pill`/`.pill-purple`/`.pill-red`.
- `index.html`: לוגו קיבל תג "PRO", נוסף `.pill-row` (✏️ סדר מוצרים / 📊 הצג סכום / 🗑️ מחק) בעמוד הרשימה, ופיל "✏️ סדר רשימות" בעמוד הרשימות (הוחלפו מכפתורי אייקון).
- `js/ui/render.js`: כותרות קטגוריה וכרטיסי פריט צבועים לפי `CATEGORIES` מ-`constants.js` (במקום צבע אחיד).
- `js/features/organize.js`: חיווט לשני הפילים החדשים — `openStatsShortcutBtn`→`setActivePage('stats')`, `openDeleteShortcutBtn`→`setItemEditMode(true)`.
- `manifest.json`/`sw.js`: theme/background color חזרו לסגול, `CACHE_NAME` הועלה ל-v3.

נבדק ב-Playwright: כל חבילת ה-QA המלאה (הוספה/עריכה/מחיקה, נעילה, מצב עריכה, גרירה, בחירה מרובה, הדפסה, autocomplete/מחירון, ייבוא/קול, חיפוש) + שני הפילים החדשים במפורש — **הכל עובד, 0 שגיאות אמיתיות**. מוזג ל-`main`.

## שלב 15: עיגון המודלים לחלק העליון של המסך (במקום התחתון)
המשתמש ביקש להעלות את מודל הוספת הפריט (ושאר המודלים, שחולקים אותן מחלקות) לחלק העליון של המסך, כדי שמקלדת אנדרואיד לא תסתיר את השדות. שינוי CSS טהור ב-`style.css`, בלי JS/HTML:
- `.modal-backdrop`: `align-items: flex-end` → `flex-start`.
- `.modal-card`: `border-radius` הפוך (מעוגל למעלה→מעוגל למטה: `0 0 var(--radius-lg) var(--radius-lg)`), `padding-bottom: calc(...+safe-area-inset-bottom)` הוחלף ב-`padding-top: calc(...+safe-area-inset-top)`.
- `--shadow-lg` (טוקן גלובלי, בשימוש בלעדי ע"י `.modal-card`): כיוון הצל הפוך מ-`0 -15px...` ל-`0 15px...`.

נבדק ב-Playwright: כל 6 המודלים (`itemModal`/`listModal`/`historyModal`/`importModal`/`macheronModal`/`confirmModal`) מעוגנים כעת ב-y=0 (חלק עליון). סימולציית "מקלדת אנדרואיד" (כיווץ viewport ל-420px גובה) אישרה ששדה שם הפריט נשאר גלוי (y=68) ולא מוסתר. הרצת רגרסיה מלאה (הוספת פריט, מצב עריכה+בחירה מרובה, נעילה) — **0 שגיאות**. מוזג ל-`main`.

## שלב 16: הסרת כפתור "תקציב" מסרגל התחתון
המשתמש ביקש למחוק את כפתור "תקציב" (השלישי בסרגל התחתון) ואת תפקידו. הובהר מולו שהכפתור מוביל בפועל לעמוד הסטטיסטיקה (`pageStats`), וקיים כפתור נוסף ("📊 הצג סכום" בשורת הפילים בעמוד הרשימה) שמוביל לאותו עמוד — המשתמש בחר להסיר **רק את כפתור הסרגל**, ולהשאיר את עמוד הסטטיסטיקה נגיש דרך הפיל הקיים.
- `index.html`: הוסרה שורת `<button id="tabStats" ...>תקציב</button>` מ-`nav.tab-bar`. סרגל התחתון נשאר עם "רשימה"/"רשימות" + כפתור ה-FAB "+ פריט".
- לא נדרש שינוי ב-JS: `initNavTabs` ב-`js/app.js` והלוגיקה ב-`js/ui/render.js` (`setActivePage`) גנריים לחלוטין (עובדים לפי `data-page`/`.tab-btn` הקיימים, עם `?.` בטוח כשלא קיים `tabStats`).
- `pageStats`, `js/features/stats.js`, קיצור-הדרך `openStatsShortcutBtn` וקיצור ה-manifest (`?page=stats`) נשארו ללא שינוי — עמוד הסטטיסטיקה עדיין נגיש ועובד.

נבדק ב-Playwright: הסרגל מציג רק 2 טאבים + FAB, `#tabStats` לא קיים ב-DOM, לחיצה על "הצג סכום" עדיין פותחת את `pageStats` כרגיל, ומעבר חזרה ל-"רשימה" עובד. הרצת רגרסיה מלאה (כל 6 המודלים, הוספת פריט, מצב עריכה, נעילה) — **0 שגיאות**. מוזג ל-`main`.

## שלב 17: איזון מחדש של סרגל התחתון (אחרי הסרת "תקציב")
לאחר הסרת כפתור "תקציב" (שלב 16), הסרגל התחתון נראה לא מאוזן — שני הטאבים הנותרים התקבצו רחוק מה-FAB עם רווח מת גדול. עוצב פתרון ב-`/design` (קנבס נפרד, כמה סבבי איטרציה עם המשתמש: מיקוד מחדש → מתיחת הטאבים למילוי המרחב → תיקון סדר ה-FAB → איזון רווחים שווים), ולבסוף שולב בקוד האמיתי:
- `style.css`: `.tab-btn` קיבל `flex:1; text-align:center;` (הטאבים נמתחים וממלאים את השטח הפנוי) ו-padding עודכן ל-`9px 14px`. `.fab-btn` — הוסר `margin:0 0 0 auto` (מיותר כעת; מכיוון ש-`fabAddItem` עדיין ראשון ב-DOM וה-RTL ממקם אותו מימין, ושני הטאבים עם `flex:1` ממלאים אוטומטית את שאר השטח בלי צורך בטריק ה-margin). לא נדרש שינוי ב-`index.html` — המבנה הקיים (FAB ראשון, שני tab-btn כילדים ישירים עם `gap:10px` על `.tab-bar`) כבר תאם בדיוק לעיצוב שאושר.

נבדק ב-Playwright: הרווח בין FAB↔"רשימה" ובין "רשימה"↔"רשימות" שווה בדיוק (10px כל אחד), ה-FAB צמוד לקצה הימני (374px = 390-16 padding), מעבר בין טאבים/פתיחת מודל הוספה עדיין עובדים. הרצת רגרסיה מלאה (6 מודלים, הוספת פריט, מצב עריכה, נעילה) — **0 שגיאות**. מוזג ל-`main`.

## שלב 18: גובה קבוע למודל "הוספת פריט" גם כשמופיעות הצעות autocomplete
המשתמש ביקש שחלונית הוספת הפריט תישאר באותו גובה גם כשמקלידים שם מוצר (ה-autocomplete דחף בעבר את שאר השדות למטה וגרם לחלונית לגדול). הפתרון: הפיכת `#autocompleteContainer` מרשימה שיושבת בזרימה הרגילה של הטופס לתפריט-צף (overlay) שמונח מעל שאר השדות במקום לדחוף אותם.
- `index.html`: שורת שם הפריט (input+כפתור קול) ו-`#autocompleteContainer` נעטפו יחד ב-`<div style="position:relative;">` חדש.
- `style.css`: ל-`.autocomplete-list` נוספו `position:absolute; top:100%; inset-inline:0; z-index:5;` + `background:var(--color-surface)` ו-`box-shadow:var(--shadow-md)` (כדי שהרשימה הצפה תהיה אטומה ותיראה מונחת מעל השדות, לא שקופה).

נבדק ב-Playwright: גובה `.modal-card` נשאר זהה לחלוטין (321px) לפני ואחרי שהצעות ה-autocomplete מופיעות, בחירת הצעה עדיין ממלאת שם/מחיר נכון. הרצת רגרסיה מלאה (6 מודלים, הוספת פריט, מצב עריכה, נעילה) — **0 שגיאות**. מוזג ל-`main`.

## שלב 19: תיקון קאשינג ב-Service Worker (התיקון הקודם לא נראה במכשיר האמיתי)
המשתמש בדק את שלב 18 (autocomplete overlay) על המכשיר האמיתי שלו (Android, PWA מותקנת דרך GitHub Pages) והראה שהחלונית עדיין גדלה כשמקלידים — כלומר ה-`style.css` המעודכן לא הגיע אליו, למרות שהקוד ב-repo כבר תוקן ונבדק מקומית (Playwright אישר 0 שינוי גובה). האבחנה: `sw.js` עשה `fetch(event.request)` בלי לציין מצב cache, ולכן ה-fetch כפוף ל-**HTTP cache הרגיל של הדפדפן** (Cache-Control מ-GitHub Pages) — גם כשה-Service Worker "network-first", אם הדפדפן עצמו מחזיק תשובה "טרייה" (בתוך ה-max-age), הוא לא בכלל פונה לרשת. זו לא הייתה בעיה בקוד העיצוב עצמו, אלא בעיית רענון קאש.
- `sw.js`: שינוי ל-`fetch(event.request, { cache: "no-store" })` — עוקף לגמרי את ה-HTTP cache של הדפדפן בכל בקשה, כך שכל שינוי עתידי ב-CSS/JS/HTML יגיע מיידית ללקוחות מחוברים לרשת (ה-cache API הפנימי של ה-SW נשאר כרשת גיבוי אופליין בלבד, לא מושפע).
- `CACHE_NAME` הועלה ל-`vplus-v4` (ניקוי cache ישן, כמקובל בפרויקט).

**חשוב למשתמש**: לאחר המיזוג, כדי לראות את התיקון במכשיר יש לבצע רענון מלא (סגירת ה-PWA לגמרי ופתיחה מחדש, או "רענון קשיח" בדפדפן) — פעם אחת בלבד; מכאן ואילך עדכוני קוד יגיעו אוטומטית בלי הצורך בכך.

## שלב 20: "הוספה רציפה" ו-"מתווסף אל" במודל הוספת פריט
המשתמש ביקש להוסיף שני פיצ'רים מהאפליקציה המקורית שעדיין חסרו ב-vplus, לפי צילום מסך: **⚡ הוספה רציפה** (מתג — כשמופעל, אחרי שמירה הטופס מתאפס אבל המודל נשאר פתוח להוספת פריט הבא, בלי לסגור) ו-**➕ מתווסף אל:** (בורר רשימת יעד עם dropdown ויצירת רשימה חדשה inline, בלי לצאת מהמודל). נבדק קוד המקור (`/workspace/shopping-list/script.js`) ויושם בהתאמה ל-vplus:
- `js/core/store.js`: ל-`createList(name, options)` נוסף פרמטר אופציונלי `{ switchCurrent = true }` — כשיוצרים רשימה מתוך ה-dropdown (`switchCurrent:false`), הרשימה הפעילה הנוכחית לא משתנה עד לשמירה בפועל של הפריט (3 הקריאות הקיימות ממשיכות לעבוד ללא שינוי, ברירת המחדל נשארה `true`).
- `index.html`: ב-`#itemModal` נוספו `.quick-add-header` (המתג + הכותרת) ו-`.context-bar` (`#contextListBtn` + `#listDropdown` עם `#listDropdownScroll` ושורת יצירת רשימה `#newListFromDropdown`/`#createListFromDropdownBtn`) לפני ה-`<form>`. כפתור השמירה קיבל `id="itemSaveBtn"` לעדכון טקסט דינמי.
- `style.css`: נוספו כללי `.quick-add-header`/`.continuous-toggle-wrap`/`.toggle-switch`/`.context-bar`/`.context-list-btn`/`.list-dropdown*` — מועתקים מהערכים המדויקים של המקור, ממופים לטוקנים הקיימים של vplus היכן שמתאים.
- `js/features/item-crud.js`: מצב מודול (`targetListId`, `continuousAdd` עם persistence ב-`localStorage['vplus_continuous_add']`), פונקציות dropdown (פתיחה/סגירה/בחירה/יצירת רשימה, כולל סגירה בלחיצה מחוץ ל-context-bar), `toggleContinuousMode()`. `openAddItemModal()` מציג את הבקרות החדשות ומאתחל אותן; `openEditItemModal()` **מסתיר** אותן (לא רלוונטיות בעריכה). `handleItemFormSubmit()` בהוספה: בודק נעילה על רשימת **היעד** (לא רק הפעילה), מוסיף אליה, מעביר את הרשימה הפעילה אליה, ולפי מצב הטוגל — משאיר את המודל פתוח ומאפס טופס (רציף) או סוגר כרגיל.

נבדק ב-Playwright: ברירת מחדל של רשימת היעד = הרשימה הפעילה; הפעלת "הוספה רציפה" + הוספת 2 פריטים ברצף בלי שהמודל נסגר, טקסט הכפתור מתעדכן, כיבוי המתג סוגר כרגיל; פתיחת ה-dropdown ובחירת רשימת יעד אחרת → הפריט אכן נוסף שם והתצוגה עברה לרשימה הזו; יצירת רשימה חדשה מתוך ה-dropdown לא משנה את הרשימה המוצגת עד לשמירה בפועל; ניסיון הוספה לרשימת יעד נעולה נחסם עם טוסט. הרצת חבילת הרגרסיה המלאה (6 מודלים, autocomplete overlay, מצב עריכה, נעילה) — **0 שגיאות**. מוזג ל-`main`.

## שלב 21: דחיסת גובה מודל "הוספת פריט" (שמירת כל האלמנטים)
המשתמש שלח צילום של הגובה החדש שנוצר לאחר הוספת "הוספה רציפה"/"מתווסף אל" (שלב 20) וביקש לצמצם את הגובה בלי להסיר אלמנטים. תוקן ב-`style.css` בלבד, ממוקד לסלקטורים החדשים בלבד (לא נגעתי בכללי `label`/`input` הגלובליים):
- `.quick-add-header` — `margin-bottom: 10px` → `8px`.
- `.context-bar` — `margin-bottom: 14px` → `8px` (זו הייתה סטייה מהמקור — ב-`/workspace/shopping-list` הערך המקורי הוא `8px`; התיקון גם החזיר התאמה פיקסלית וגם דחס).
- `.context-bar` — `padding: 6px 10px` → `5px 10px`.
- נוסף `label[for="itemName"] { margin-top: 4px; }` — במקום הרווח הגלובלי (`10px`) בין ה-context-bar לשדה "שם הפריט" בלבד. **הערה טכנית**: ניסיון ראשון עם `.context-bar + label` נכשל בשקט כי ה-label נמצא בתוך `<form>` שהוא ה-sibling האמיתי של `.context-bar` (לא ה-label עצמו) — תוקן לסלקטור ישיר לפי `for="itemName"`.

נבדק ב-Playwright: גובה `.modal-card` ירד מ-387px ל-377px, כל האלמנטים (מתג, context-bar, dropdown) עדיין קריאים ונוחים ללחיצה. הרצת כל הרגרסיות הרלוונטיות (6 מודלים, autocomplete overlay, הוספה רציפה, בחירת רשימת יעד + יצירת רשימה מה-dropdown) — **0 שגיאות**. מוזג ל-`main`.

## שלב 22: השוואה ישירה מול המודל המקורי — צמצום אמיתי של גובה המודלים
המשתמש ביקש להשוות ישירות מול חלונית ה-`.edit-modal` של shopping-list ולתקן. השוואת ערכים מדויקת (`/workspace/shopping-list/style.css` שורות 202-214, 322-348; `index.html` שורה 3519) גילתה שהפער העיקרי לא היה בשורות החדשות (שתוקנו בשלב 21) אלא ב-**מסגרת המודל המשותפת עצמה**: המקור משתמש ב-`.edit-modal { padding: 6px 25px; }` וב-`margin-top: 4px` לשורת כפתורי הפעולה, בעוד ב-vplus `.modal-card` היה עם `padding: 16px` (+ safe-area) ו-`.modal-actions { margin-top: 16px; }` — פער גדול משמעותית מהמקור.
- `style.css`: `.modal-card` — `padding: 16px` → `padding: 8px 16px` (רק אנכי צומצם; אופקי נשאר 16px, קרוב מספיק ל-25px של המקור בלי לשבש רוחב תוכן קיים), `padding-top: calc(16px+safe-area)` → `calc(8px+safe-area)`.
- `.modal-actions` — `margin-top: 16px` → `4px` (התאמה מדויקת למקור).
- אלו כללים **משותפים לכל 6 המודלים** (הוספת/עריכת פריט, רשימה, היסטוריה, ייבוא, מחירון, אישור) — בדיוק כמו שב-shopping-list `.edit-modal` משותף לכולם; השינוי מתאים את כולם באופן עקבי, לא רק את מודל הפריט.
- **הוחלט במפורש לא לגעת ב**: תוויות `<label>` האמיתיות מעל כל שדה (בניגוד למקור שמשתמש רק ב-placeholder בתוך השדה) — אלו נדרשות לפי הנחיית הנגישות (IS 5568/WCAG) ב-`CLAUDE.md` ונשארות, גם אם זה אומר שהמודל לא יגיע לגובה המדויק של המקור לחלוטין.

נבדק ב-Playwright: גובה `.modal-card` של מודל הוספת פריט ירד מ-387px (לפני שלב 21) → 377px (אחרי שלב 21) → **349px** (אחרי השלב הזה). כל 5 המודלים האחרים נבדקו ויזואלית — נשארים קריאים ונוחים, בלי שבירה. הרצת כל חבילות הרגרסיה (6 מודלים, autocomplete overlay, הוספה רציפה, בחירת רשימת יעד) — **0 שגיאות**. מוזג ל-`main`.

## שלב 23: שדות "שם פריט"/"מחיר"/"כמות" כ-placeholder עם label נגיש מוסתר
המשתמש הראה השוואה ישירה מול shopping-list וציין שהגובה הנמוך של המקור מגיע מכך ששלושת השדות (שם/מחיר/כמות) משתמשים ב-placeholder בתוך השדה במקום תווית נפרדת מעליו. הובהר קונפליקט מול הנחיית CLAUDE.md (טפסים דורשים תוויות אמיתיות לפי IS 5568/WCAG — placeholder בלבד פוסל), והמשתמש בחר בפתרון המומלץ: **label אמיתי ב-DOM (מקושר `for`/`id`, נגיש לקורא-מסך) אבל מוסתר ויזואלית (`sr-only`)**, עם placeholder גלוי במקומו.
- `style.css`: נוסף `.sr-only` (utility ויזואלית-מוסתר סטנדרטי), `#itemModal .quick-input` (כלל ממוקד למודל הפריט בלבד — לא נוגע ב-`input[type=...]` הגלובלי שמשמש בכל שאר האפליקציה; מדמה את `.modal-input` המקורי: `border-radius:16px`, `text-align:center`, `font-weight:700`, גבול תואם בדיוק ל-`var(--color-neutral-900)`=`#f3f4f6` כמו במקור), ו-`.quick-form-row { margin-top:8px }` לשמירת ריווח אחרי שהתוויות הגלויות הוסרו מהזרימה.
- `index.html`: תוויות `itemName`/`itemPrice`/`itemQty` קיבלו `class="sr-only"` (נשארות ב-DOM), השדות עצמם קיבלו `placeholder` (בהתאמה: "שם הפריט"/"💰 מחיר"/"🔢 כמות", שומר על מינוח "פריט" העקבי של vplus) ו-`class="quick-input"`. שדה הקטגוריה נשאר ללא שינוי (מחוץ להיקף — לא הוזכר, ובמקור בכלל מוסתר מאחורי "פרטים נוספים").

נבדק ב-Playwright: גובה `.modal-card` ירד מ-349px ל-**331px**. אימות נגישות מפורש — `label[for="itemName"]` עדיין קיים ב-DOM עם bounding box של 1×1px (מוסתר ויזואלית, לא `display:none`), ו-`input.labels[0].textContent` מחזיר את הטקסט הנכון (קורא-מסך יקבל הקשר תקין). בדיקה פונקציונלית מלאה: הוספה/עריכה של פריט, autocomplete overlay, הוספה רציפה, בחירת רשימת יעד — כולם עדיין עובדים. הרצת כל חבילות הרגרסיה — **0 שגיאות**. מוזג ל-`main`.

## שלב 24: נגישות טקסט — כפתור הגדלת אותיות + מעבר ל-rem
המשתמש ביקש טיפול בנגישות עבור מי שמתקשה לקרוא אותיות קטנות. יושמה הגישה המומלצת (בסיס תשתיתי + כפתור באפליקציה, בלי מסך הגדרות נפרד):
- `style.css`: **כל** הופעות `font-size`/הרכיב ב-`font: weight Npx` הומרו שיטתית ל-`rem` (חלוקה ב-16) בכל הקובץ — היקף מכוון: רק טיפוגרפיה, לא ריפוד/מרווחים/רדיוסים/גדלי אייקונים, כדי לא לשבש פריסה שכוונה בקפידה. `html,body` פוצל: `html{font-size:100%}` (שורש לא-נעול, מאפשר לנגישות המערכת של המכשיר לפעול), `body{font-size:0.9375rem}` (15px בברירת מחדל — שומר על המראה הקיים במדויק). נוספו `html[data-text-scale="2"]{font-size:112%}` ו-`html[data-text-scale="3"]{font-size:125%}`.
- `index.html`: כפתור `#textScaleBtn` (אייקון `ph-text-aa`) נוסף ל-`.toolbar-row` הקבוע בהדר (מופיע בכל העמודים). נוסף סקריפט inline סינכרוני בראש ה-`<head>` שקורא את הרמה מ-`localStorage` וקובע `data-text-scale` על `<html>` **לפני** הרינדור הראשון (מונע הבהוב).
- `js/features/text-scale.js` (חדש) — `initTextScale()`: מחזר בין 3 רמות (רגיל/גדול/גדול מאוד) בלחיצה, שומר ב-`localStorage['vplus_text_scale']`, מעדכן `title`/`aria-label`/קלאס `active`.
- `sw.js` — הקובץ החדש נוסף ל-precache, `CACHE_NAME` הועלה ל-`vplus-v5`.

נבדק ב-Playwright: כל 3 הרמות (100%/112%/125%) מגדילות טקסט פרופורציונלית בכל האתר (נבדק על `.item-name` בפועל: 13.5px→15.12px→16.875px), חזרה לרגיל עובדת, מצב נשמר בין רענוני דף בלי הבהוב, הפריסה לא נשברת גם ברמה הגבוהה ביותר (נבדק ויזואלית). הרצת כל חבילות הרגרסיה הקיימות (6 מודלים, autocomplete overlay, הוספה רציפה, בחירת רשימת יעד) — **0 שגיאות**. מוזג ל-`main`.

## שלב 25: החלפת פונט ברירת המחדל ל-Secular One
המשתמש ביקש להחליף את פונט ברירת המחדל (Assistant). עוצבו 5 חלופות בקנבס `/design` (Heebo/Rubik/Noto Sans Hebrew/Secular One/Frank Ruhl Libre) על אותו תוכן מייצג להשוואה ישירה — המשתמש בחר **Secular One** ושילב בקוד:
- `index.html`: קישור ה-Google Fonts הוחלף מ-Assistant ל-`family=Secular+One`.
- `style.css`: `--font-heading`/`--font-body` הוחלפו ל-`"Secular One", "Assistant", system-ui, sans-serif` (Assistant נשאר כ-fallback למקרה שה-CDN לא נטען, לא כברירת מחדל).

נבדק ב-Playwright: `getComputedStyle(document.body).fontFamily` מאשר את הפונט החדש, צילום מסך אישר ויזואלית שהפונט הבולט/הגיאומטרי מוחל בכל האפליקציה (לוגו, כותרות, פילים, כפתורים). הרצת כל חבילות הרגרסיה — **0 שגיאות**. מוזג ל-`main`.

## שלב 26: קיבוע גובה מודל "הוספת פריט" בכל רמות הגדלת הטקסט
המשתמש ביקש שגובה מודל הוספת פריט יישאר זהה בין כל 3 רמות הגדלת הטקסט (שלב 24), במקום לתפוח כשה-rem גדל. נמדד גובה טבעי בפועל: רמה 1=331px, רמה 2=342px, רמה 3=359px (עם הפונט Secular One הנוכחי).
- `style.css`: נוסף כלל ממוקד `#itemModal .modal-card { max-height: min(335px, 88vh/88dvh); }` — תקרה קבועה בפיקסלים (לא יחסית ל-rem או ל-viewport) ספציפית למודל הוספת/עריכת פריט בלבד. ה-`overflow-y:auto` הקיים כבר על `.modal-card` מאפשר גלילה פנימית כשהתוכן (ברמות 2-3) גדול מהתקרה.

נבדק ב-Playwright: גובה בפועל — רמה 1=331px (מתחת לתקרה, לא מושפע), רמה 2=335px, רמה 3=335px (שתיהן מוגבלות לתקרה) — כלומר הפרש של 4px בלבד לעומת ~28px לפני התיקון. אומת שניתן עדיין לגלול בתוך המודל ברמה 3 ולהגיע לכפתור השמירה ולהוסיף פריט בהצלחה. הרצת כל חבילות הרגרסיה — **0 שגיאות**. מוזג ל-`main`.

## שלב 27: תיקון סימן ה-V ההפוך בכפתור פריט שנאסף
המשתמש שם לב שסימן ה-"✓" בכפתור העגול של פריט מסומן נראה הפוך. אבחון: `.item-checkbox.checked::after` (טריק ה-CSS הקלאסי לציור "✓" — border שמאל+תחתון מוסר עליון+ימין, מסובב 45deg) השתמש ב-`border-inline-end: none` (תכונה **לוגית**, תלוית-כיוון) במקום `border-right: none` (**פיזית**). ב-RTL, `border-inline-end` ממופה ל-**שמאל** ולא ימין — כך שבפועל הוסר עליון+שמאל במקום עליון+ימין, ונוצרה צורה הפוכה. תוקן ל-`border-right: none` — צורת הסימן היא גרפיקה קבועה שלא אמורה להתהפך לפי כיוון הטקסט (בניגוד ל-`border-inline-start` של פס הצבע בכרטיס הפריט, שנשאר לוגי בכוונה כי הוא כן אמור לזוז לפי כיוון).

נבדק ב-Playwright: סימון פריט וצילום מסך מקרוב מאשרים "✓" תקין (קודקוד בתחתית-שמאל, פונה לפינה עליונה-ימנית). הרצת חבילת הרגרסיה — **0 שגיאות**. מוזג ל-`main`.

## שלב 28: לוגו ההדר משתמש באייקון האמיתי של האפליקציה
המשתמש שלח צילום מסך של אייקון ה-PWA (הריבוע הסגול המעוגל עם ה-V הירוק, שנוצר בשלב 9) וביקש שהלוגו בהדר האפליקציה ישתמש באותו אייקון במקום אות "V" טקסטואלית פשוטה.
- `index.html`: `<span class="logo-v">V</span>` הוחלף ב-`<img src="assets/icons/icon-96.png" alt="" class="logo-icon">` (alt ריק — דקורטיבי, הטקסט "plus" הסמוך כבר מספק הקשר).
- `style.css`: `.logo-v` (כלל צבע לא רלוונטי יותר) הוחלף ב-`.logo-icon` (24×24px, `border-radius:6px`, `vertical-align:middle` לשמירה על יישור עם הטקסט ועם ה-superscript "PRO" הסמוך שממשיך להסתמך על `vertical-align:super` בזרימת טקסט רגילה — לכן `.app-logo` עצמו לא הפך ל-flex).

נבדק ב-Playwright: האייקון נטען בהצלחה (`naturalWidth>0`, ללא בקשות כושלות), צילום מסך אישר ויזואלית שהלוגו תואם בדיוק לצילום ששלח המשתמש. הרצת חבילת הרגרסיה — **0 שגיאות**. מוזג ל-`main`.

## שלב 29: תיקון פרשנות — ביטול שינוי לוגו ההדר, והחלפת ה-V באייקון ה-PWA לסימן וי ירוק
המשתמש הבהיר ששלב 28 היה טעות פרשנות: הוא לא רצה שלוגו ההדר ישתמש באייקון האפליקציה, אלא ש**אייקון ה-PWA עצמו** (קובצי ה-PNG תחת `assets/icons/`, מה שכרום מציג בהתקנה) יחליף את אות ה-"V" הפנימית שלו בסימן **✓ (וי)** בצבע ירוק.
- **ביטול שלב 28**: `index.html`/`style.css` הוחזרו ל-`<span class="logo-v">V</span>` (טקסט) בהדר, `.logo-icon` הוסר.
- **יצירת האייקונים מחדש**: בסביבת ה-sandbox אין Pillow/ImageMagick/rsvg-convert/inkscape מותקנים — נבנה דף HTML קטן (רקע גרדיאנט זהה לקיים `135deg,#7367f0→#9055ff`, פינות מעוגלות 22%, סימן ✓ SVG ירוק `#22c55e` stroke-based בתוך אזור בטוח למרכז, חשוב כי `icon-192`/`icon-512` מסומנים `maskable` ב-`manifest.json`) ו-Playwright צילם אותו ב-8 הגדלים המקוריים (72-512px) ישירות לתוך `assets/icons/` תחת אותם שמות בדיוק, כולל `icon.png` (זהה ל-512). לא נדרש שינוי ב-`manifest.json`/`sw.js`.

נבדק ב-Playwright: ההדר חזר בדיוק למראה המקורי (`V plus` טקסטואלי), האייקונים נבדקו ויזואלית בכמה גדלים (72/192/512) — הסימן ברור וממורכז גם בגודל הקטן ביותר. הרצת חבילת הרגרסיה — **0 שגיאות**. מוזג ל-`main`.

## שלב 30: כפתור "שתף רשימה"
עוצבו 5 אפשרויות לכפתור שיתוף רשימה בקנבס `/design` (עגול תואם לסרגל / פיל עם טקסט / גרדיאנט בולט / ירוק כמו FAB / קישור טקסט מינימלי), עם אייקון "share-nodes" מועתק 1:1 מ-`/workspace/shopping-list`. המשתמש בחר **אופציה 2 (פיל עם טקסט)** ושילב בקוד:
- `index.html`: נוסף `<button id="shareListBtn" class="pill pill-purple">📤 שתף רשימה</button>` בתוך ה-`.pill-row` הקיים של `#pageLists` (בין "סדר מוצרים" ל-"הצג סכום").
- `js/features/share.js` (חדש): `initShare()` — בונה טקסט שיתוף באותו פורמט בדיוק כמו `shareNative('list')` המקורי (`🛒 *שם הרשימה:*` + שורה לכל פריט עם ✅/⬜, שם, כמות ומחיר + סה"כ בסוף), קורא ל-`navigator.share()` (פותח את תפריט השיתוף האמיתי של המכשיר, כולל WhatsApp); fallback להעתקה ללוח (`navigator.clipboard.writeText`) כשה-API לא זמין; רשימה ריקה מציגה טוסט "הרשימה ריקה" בלי לנסות לשתף; ביטול (AbortError) לא מציג שגיאה.
- `js/app.js`: `initShare()` מיובא ומופעל.
- `sw.js`: הקובץ החדש נוסף ל-precache, `CACHE_NAME` הועלה ל-`vplus-v6`.

נבדק ב-Playwright: הפיל מוצג נכון בשורת הפילים, מקרה רשימה ריקה מציג טוסט בלי לקרוא ל-share, `navigator.share()` נקרא עם title/text בפורמט נכון (אומת עם פריט מסומן+לא מסומן), fallback להעתקה ללוח עובד ומציג טוסט. הרצת חבילת הרגרסיה — **0 שגיאות**. מוזג ל-`main`.

## שלב 31: כפתור השיתוף — אייקון מינימליסטי בלבד, בלי טקסט
המשתמש ביקש להסיר את הטקסט "שתף רשימה" מהפיל ולהשאיר רק אייקון שיתוף מינימליסטי בסגול, על אותו רקע.
- `index.html`: תוכן `#shareListBtn` הוחלף מ-`📤 שתף רשימה` ל-`<i class="ph ph-share"></i>` בלבד (אייקון Phosphor סטנדרטי, תואם למוסכמה הקיימת של שאר כפתורי האייקון באפליקציה). `aria-label`/`title="שתף רשימה"` נשארו ללא שינוי — הכפתור עדיין נגיש לקורא-מסך גם בלי טקסט גלוי.
- `style.css`: נוסף `#shareListBtn { font-size: 1rem; line-height: 1; }` כדי שהאייקון יהיה בגודל ברור בתוך הפיל (במקום 0.75rem הרגיל של טקסט פיל).

נבדק ב-Playwright: תוכן הטקסט של הכפתור ריק, האייקון קיים ב-DOM, הלחיצה עדיין מפעילה את `navigator.share()` עם הפורמט הנכון. **הערה**: בסביבת ה-sandbox הזו ה-CDN של Phosphor Icons חסום (מגבלה ידועה שכבר תועדה בפרויקט) — לכן האייקון לא נראה ויזואלית בצילומי המסך המקומיים, אך יוצג נכון אצל משתמשי קצה אמיתיים. הרצת חבילת הרגרסיה — **0 שגיאות**. מוזג ל-`main`.

## שלב 32: טקסט שיתוף עשיר — קיבוץ לפי קטגוריה בפורמט WhatsApp
המשתמש ביקש עיצוב "מרשים" לשיתוף דרך וואטסאפ שידמה לאפליקציה עצמה (`/design`). עוצב קנבס "כרטיס שיתוף" ויזואלי (כרטיסי פריט צבעוניים, סרגל סיכום — כמו `.item-card`/`.list-totals-bar` האמיתיים), אך זה דורש ציור ל-`<canvas>` ושיתוף כקובץ תמונה — היקף גדול יותר. המשתמש ביקש במפורש להישאר עם **טקסט רגיל** (לא תמונה), ולכן הועשר הפורמט הטקסטואלי הקיים (משלב 30) כדי לקרב אותו ל"תחושת" האפליקציה תוך שימוש בעיצוב טקסט של וואטסאפ בלבד (`*מודגש*`, `~קו חוצה~`) וקיבוץ לפי קטגוריה:
- `js/features/share.js`: `buildShareText()` נכתב מחדש — כותרת רשימה מודגשת + "שותף מ-vplus" נטוי, פריטים לא-נאספים מקובצים לפי קטגוריה (כותרת קטגוריה + סכום ביניים לכל קטגוריה, מוצג פעם אחת בלבד לכל קטגוריה — לא נפתח מחדש כשחוזרים אליה), פריטים נאספים מקובצים תחת כותרת משותפת יחידה "✅ נאספו · N — סכום" (ולא מפוצלים בחזרה לפי קטגוריה), מספור רץ לכל הפריטים, שם פריט לא-נאסף `*מודגש*` ונאסף `~קו חוצה~`, קו מפריד + סה"כ כולל מודגש בסוף.
- **תוקן באותו שלב** (נמצא תוך כדי בדיקה): גרסה ראשונית שגתה בקיבוץ — כיוון ש-`sortItemsByStatusAndCategory` מעביר את כל הפריטים הנאספים לסוף הרשימה (בלוק אחד, לא מקובץ מחדש לפי קטגוריה), כותרת קטגוריה עם גם פריט נאסף וגם לא-נאסף (למשל "🥛 חלב וביצים") הופיעה **פעמיים**. תוקן כך שהלוגיקה תואמת בדיוק את ההתנהגות האמיתית ב-`js/ui/render.js`'s `renderItemsPage()`: כותרת קטגוריה רק לפריטים לא-נאספים, כל הפריטים הנאספים תחת כותרת "נאספו" משותפת אחת.

נבדק ב-Playwright (רשימה עם 4 פריטים ב-3 קטגוריות, פריט אחד מסומן כנאסף): אומת שכל כותרת קטגוריה מופיעה **פעם אחת בלבד**, כל הפריטים הנאספים מופיעים תחת כותרת "✅ נאספו" משותפת יחידה, הסכומים והמספור נכונים. הרצת חבילת הרגרסיה המלאה — **0 שגיאות**. מוזג ל-`main`.

## שלב 33: תיקון באג — כפתור "הצג סכום" נתקע במסך חדש
המשתמש דיווח שכפתור "📊 הצג סכום" פותח מסך חדש (`#pageStats` — עמוד סטטיסטיקות/גרפים מלא) בלי דרך ברורה לחזור (ה-tab-bar לא מסמן אף לשונית כפעילה במצב הזה). אבחון: הכפתור היה מחובר ל-`setActivePage('stats')` — ניתוב מלא לעמוד לא-קשור, בעוד שבריפו המקורי `shopping-list`, "הצג סכום" הוא **טוגל מוטמע** (inline) שמציג/מסתיר שורת סיכום (שולם/נותר/סה"כ) **בלי לנווט לשום מקום**.

תוקן כדי להתאים ל-shopping-list, תוך שימוש בתשתית הקיימת ב-vplus (במקום ליצור שורת סיכום כפולה):
- `index.html`: `#listTotalsBar` (שכבר מציגה בדיוק את אותו מידע — שולם/נותר/סה"כ) קיבלה `hidden` כברירת מחדל.
- `js/features/organize.js`: `#openStatsShortcutBtn` הוסר מ-`setActivePage('stats')` וחובר לפונקציה חדשה `handleToggleTotalsBar()` — מטגלת `hidden` על `#listTotalsBar`, מטגלת class `active` על הכפתור (נעזר ב-`.pill.active` הקיים ב-CSS — רקע סגול מלא כשפתוח), ומחליפה טקסט/`aria-label`/`title` בין "📊 הצג סכום" ל-"✕ הסתר סכום". ה-import של `setActivePage` הוסר מהקובץ (לא נדרש עוד שם).
- עמוד `#pageStats` (הגרפים המלאים) והקיצור שלו ב-`manifest.json` (`?page=stats`, נפתח מהתקנת PWA) נשארו ללא שינוי — לא בטווח הבאג הזה.

נבדק ב-Playwright: לחיצה ראשונה חושפת את `#listTotalsBar` (`hidden` מוסר), מחליפה טקסט/aria/active, **בלי** לנווט (`#pageLists` נשאר גלוי, `#pageStats` נשאר מוסתר). לחיצה חוזרת מחזירה למצב סגור. הרצת חבילת הרגרסיה המלאה — **0 שגיאות**. מוזג ל-`main`.

## שלב 34: תיקון גלילה — סרגל התחתון (Tab Bar) נעלם בגלילה ברשימות ארוכות
המשתמש שלח צילום מסך של הסרגל התחתון (רשימות/רשימה/+ פריט) וביקש שיוצג תמיד, בלי צורך לגלול כדי לראות אותו, גם ברשימה ארוכה.

אבחון: `#app` הוגדר עם `min-height: 100vh/100dvh` (לא `height` קבועה) — כך שכשתוכן הרשימה ארוך, כל ה-`#app` (כולל ה-header וה-tab-bar, ששניהם `flex:none`) פשוט **גדל** מעבר לגובה המסך, והדפדפן גולל את **כל הדף** (body) במקום לגלול רק את תוכן הרשימה. בפועל `.page{flex:1;overflow-y:auto}` (שכבר היה מוגדר) מעולם לא נכנס לפעולה, כי `#app` לא היה מוגבל לגובה המסך בפועל.
- `style.css`: `#app` שונה מ-`min-height:100vh/100dvh` ל-`height:100vh/100dvh` קבועה — עכשיו `#app` תמיד בגובה המסך בדיוק, וה-`.page` (התוכן הגלילי) גולל **פנימית** בתוכו. ה-header, ה-pill-row, ותוצג ה-tab-bar (כולם `flex:none`) נשארים קבועים במקום ולא זזים בגלילה.
- **תוקן במקביל (רגרסיה פוטנציאלית שנמצאה תוך כדי בדיקה)**: הדפסה (`window.print()`) הייתה עלולה לקטוע רשימות ארוכות ל-viewport בלבד, כי `.page` כעת עם overflow פנימי מוגבל. נוסף ל-`@media print`: `#app{height:auto}` ו-`.page{overflow:visible;height:auto}` כדי שההדפסה תכלול את כל הרשימה כרגיל.

נבדק ב-Playwright: רשימה עם 25 פריטים — הסרגל התחתון נשאר קבוע ב-`bottom` של המסך (`position` לא זז) בכל ניסיון גלילה של החלון; הגלילה בפועל קורית בתוך אזור הרשימה (`#pageLists`) והפריט האחרון נחשף במלואו מעל הסרגל התחתון בלי חפיפה; ה-header נשאר קבוע למעלה. הדפסה: עם 20 פריטים, אומת שב-`@media print` הגובה חוזר ל-auto (`overflow:visible`, `scrollHeight` גדול מגובה המסך) — כלומר כל הרשימה תודפס במלואה. הרצת חבילת הרגרסיה המלאה — **0 שגיאות**. מוזג ל-`main`.

## שלב 35: כפתור "הוסף פריט" — עיצוב עיגול ירוק כמו shopping-list
המשתמש שלח צילום מסך של כפתור ה-"+" המקורי מריפו `shopping-list` וביקש עיצוב דומה לכפתור "הוספת פריט" (`#fabAddItem`) ב-vplus. לאחר הבהרה: עיגול ירוק גדול יותר, בתוך גבולות שורת התחתית (לא בולט מעלה כמו FAB צף), עם סימן "+" בלבד — בלי שום טקסט (לא בתוך העיגול ולא מתחתיו).
- `index.html`: הוסר הטקסט "פריט" מתוך `#fabAddItem` — נשאר רק `<i class="ph ph-plus"></i>`. `aria-label`/`title="הוסף פריט"` נשארו ללא שינוי (חובה לפי כללי הנגישות — כפתור אייקון-בלבד).
- `style.css`: `.fab-btn` — הוגדל מ-60px ל-62px (תואם למקור), הוסרו `flex-direction:column`/`gap`/`font` (לא נדרשים יותר בלי טקסט), אייקון ה-"+" הוגדל (`1.25rem→1.75rem`), נוספה אנימציית פעימה `@keyframes fabPulse` (טבעת `box-shadow` ירוקה שמתרחבת ונעלמת כל 2.4 שניות, זהה בהתנהגות ל-`smartPlusPulse` של shopping-list). ערכי `border`/`box-shadow`/`background` כבר תאמו למקור מראש ולא שונו.

נבדק ב-Playwright: הכפתור ללא טקסט גלוי, `aria-label`/`title` נשמרו, גודל 62×62px, לחיצה עדיין פותחת את `#itemModal` בהצלחה. הרצת חבילת הרגרסיה המלאה — **0 שגיאות**. מוזג ל-`main`.

## שלב 36: תיקון עובי סימן ה-"+" בכפתור "הוסף פריט"
המשתמש שם לב שסימן ה-"+" בכפתור החדש (שלב 35) דק יותר מהמקור ב-shopping-list. אבחון: ב-vplus הסימן נמשך מגופן האייקונים Phosphor במשקל "regular" (`ph ph-plus`) — דק מטבעו; המקור ב-shopping-list משתמש ב-SVG עם `stroke-width` עבה יחסית (2.8 מתוך viewBox של 24), כלומר לא ניתן לשחזור ע"י הגדלת font-size בלבד. הוספת סטיילשיט משקל "bold" נוסף מ-Phosphor נשקלה ונדחתה — קיים סיכון שהיא תדרוס את ה-`content` (קודי הגליפים) של **כל** שאר אייקוני ה-`ph` הרגילים באפליקציה (התנגשות class-selector גלובלית), וגם לא ניתנת לאימות בסביבת ה-sandbox הזו כי CDN של Phosphor חסום.

הפתרון שנבחר: החלפת סימן ה-"+" בכפתור הזה בלבד לצורה **CSS טהורה** (שני מלבנים מוצלבים עם `border-radius` בפינות, ללא תלות בגופן חיצוני כלל) — יחס עובי/אורך זהה למקור (עובי 3.5px, אורך 18px, בדיוק כמו יחס ה-stroke-width של shopping-list בקנה מידה 62px).
- `index.html`: `<i class="ph ph-plus"></i>` הוחלף ב-`<span class="plus-icon" aria-hidden="true"></span>` בתוך `#fabAddItem` (הכפתור עצמו עדיין נגיש דרך `aria-label`/`title` הקיימים — ה-span הדקורטיבי מוסתר מקוראי מסך).
- `style.css`: נוספו `.fab-btn .plus-icon` ו-`::before`/`::after` (שני פסים לבנים מוצלבים, 3.5px עובי, 18px אורך, פינות מעוגלות). `.fab-btn i { font-size: 1.75rem; }` (מיותר כעת) הוסר.

יתרון נוסף: בניגוד לאייקון Phosphor, הצורה הזו **לא תלויה ב-CDN חיצוני** — נבדקה ויזואלית ישירות ב-Playwright (לא רק תיאורטית) ואושרה כעבה וברורה, תואמת למקור.

נבדק ב-Playwright: הסימן מוצג נכון (צילום מסך מקרוב אישר עובי תואם למקור), `aria-hidden`/`aria-label` תקינים, לחיצה על הכפתור עדיין פותחת את `#itemModal`. הרצת חבילת הרגרסיה המלאה — **0 שגיאות**. מוזג ל-`main`.

## שלב 37: שורת הפילים (מחק/הצג סכום/שיתוף/סדר מוצרים) נשארת מוצגת בגלילה
המשתמש שלח צילום מסך של שורת הפילים העליונה (מחק/הצג סכום/שיתוף/סדר מוצרים) וביקש שתמיד תוצג, בלי קשר לאורך הגלילה — המשך ישיר לתיקון ה-tab-bar התחתון בשלב 34, שם `.page` קיבל גלילה פנימית משלה. מכיוון ש-`.pill-row` הוא ילד רגיל **בתוך** `.page` (לא flex:none sibling כמו ה-header/tab-bar), הוא גלל יחד עם רשימת הפריטים.
- `style.css`: `.pill-row` קיבל `position: sticky; top: 0; z-index: 5;` + `background` תואם לגרדיאנט הרקע הכללי (`linear-gradient(135deg,#f4f7fa,#e8ecf3)`) כדי שכרטיסי הפריטים הגוללים מתחתיו לא "יראו" דרכו. השינוי חל דרך ה-class המשותף — גם שורת הפילים בעמוד "רשימות" (סדר רשימות) נהנית מאותה התנהגות באופן טבעי.

נבדק ב-Playwright: רשימה עם 20 פריטים — לאחר גלילה פנימית של 500px, שורת הפילים נשארת בדיוק באותו מיקום במסך (ללא תזוזה), כפתורי הפילים עדיין לחיצים ובתוך ה-viewport; צילום מסך אישר ויזואלית שאין חפיפה/קרע בין השורה לכרטיסים הגוללים מתחתיה. נבדק גם בעמוד "רשימות" (Summary) עם 15 רשימות — השורה נדבקת נכון לראש אזור הגלילה לאחר שתיבת החיפוש שמעליה גוללת מחוץ לתצוגה. הרצת חבילת הרגרסיה המלאה — **0 שגיאות**. מוזג ל-`main`.

## שלב 38: תיקון מיקום שחזור פריט שנמחק (Undo) — התאמה ל-shopping-list
המשתמש ביקש שלחיצה על ה-X למחיקת פריט תציג התראה עם אפשרות ביטול, בדומה ל-shopping-list. בדיקה העלתה שהפיצ'ר הבסיסי **כבר קיים** ב-vplus (`js/features/item-crud.js` — מחיקה מיידית + טוסט "נמחק" עם כפתור "בטל", 5 שניות, בדיוק כמו במקור). אימות מול shopping-list בפועל (`script.js:4555-4596` — `removeItem`/`undoDelete`) העלה פער אמיתי אחד: המקור משחזר פריט מבוטל **בדיוק במיקום המקורי שלו במערך** (`splice(deletedItemIndex, 0, deletedItem)`), בעוד ב-vplus הביטול היה קורא ל-`addItem()` שתמיד מוסיף (`push`) **לסוף** הרשימה — כך שביטול מחיקה של פריט שלא היה אחרון "קפץ" לסוף הקטגוריה שלו במקום לחזור למקומו (משמעותי כי המיון בתצוגה יציב לפי `sortItemsByStatusAndCategory`, ששומר על סדר יחסי בתוך אותה קטגוריה/סטטוס).
- `js/core/store.js`: נוספה `insertItem(listId, index, itemData)` — מכניסה פריט למערך במיקום ספציפי (`splice`) במקום להוסיף לסוף, עם אותו `normalizeItem`/`pricebook` update כמו `addItem`.
- `js/features/item-crud.js`: לחיצה על מחיקה שומרת גם את **האינדקס המקורי** של הפריט (`snapshotIndex`), ולא רק את הנתונים שלו; פונקציית ה-`onUndo` קוראת ל-`insertItem(db.currentId, snapshotIndex, snapshot)` במקום ל-`addItem`, כדי לשחזר בדיוק למקום המקורי.

נבדק ב-Playwright: 3 פריטים באותה קטגוריה (א/ב/ג), מחיקת האמצעי ("ב") וביטול — הרשימה חוזרת בדיוק לסדר המקורי (א/ב/ג), לא (א/ג/ב) כפי שהיה קורה לפני התיקון. הרצת חבילת הרגרסיה המלאה — **0 שגיאות**. מוזג ל-`main`.

## שלב 39: הטוסט "נמחק" מופיע ממש על מיקום הפריט שנמחק
המשתמש ביקש שהטוסט "נמחק" עם כפתור "בטל" יופיע במיקום המדויק של הפריט שנמחק (לא בסרגל קבוע בתחתית המסך כמו קודם).
- `js/ui/modals.js`: `showToast()` קיבל פרמטר אופציונלי חדש `anchorRect` — כשמועבר, הטוסט ממוקם ב-`position:fixed` בדיוק על ה-rect שהתקבל (top/left/width של הפריט שנמחק) במקום המיקום הקבוע בתחתית; כשלא מועבר (כל שאר קריאות `showToast` הקיימות באפליקציה — העתקה ללוח, שיתוף וכו') מתבצע איפוס מפורש של הסטיילים המוטבעים כדי לחזור בדיוק להתנהגות ברירת המחדל (`bottom:84px` וכו').
- `js/features/item-crud.js`: לפני קריאה ל-`deleteItem`, נשמר `card.getBoundingClientRect()` של כרטיס הפריט שנלחץ, ומועבר כ-`anchorRect` ל-`showToast`.

נבדק ב-Playwright: מחיקת "פריט ב" מתוך שלושה — ה-rect של הטוסט תואם (±2px) בדיוק למיקום/רוחב כרטיס הפריט שנמחק (צילום מסך אישר ויזואלית שהטוסט "יושב" בדיוק במקום שהכרטיס היה, בין השכנים שלו). אומת גם שטוסט רגיל (לא מעוגן) בקריאה שלאחר מכן חוזר נכון למיקום ברירת המחדל בתחתית המסך. הרצת חבילת הרגרסיה המלאה — **0 שגיאות**. מוזג ל-`main`.

## שלב 40: הצגת שם פריט מלא (חתוך) בלחיצה, בלי לפתוח מודל עריכה
המשתמש גילה בטעות שהטוסט "נמחק" מציג את שם הפריט המלא גם כשהוא חתוך בכרטיס (`text-overflow:ellipsis`), וביקש דרך לראות את השם המלא בלי למחוק — ע"י לחיצה/בחירה של הפריט. בדיקה העלתה שלחיצה על גוף הפריט כבר פותחת את מסך העריכה המלא (ששם רואים את השם), אך זו חוויה כבדה מדי רק כדי לקרוא שם. הוחלט (לאחר הבהרה עם המשתמש): "הרחבה במקום" — לחיצה על שם הפריט עצמו (ולא על שאר הכרטיס) מבטלת את החיתוך באותו כרטיס, בלי לפתוח מודל.
- `js/ui/render.js`: `.item-name` (במצב לא-עריכה) קיבל `data-action="expand-name"` משלו (עוקף את ה-`data-action="edit"` של ה-`.item-main` האב כשלוחצים ישירות על השם, דרך `closest('[data-action]')` הקיים) + `title="{שם מלא}"` לטולטיפ hover חינמי לעכבר.
- `js/features/item-crud.js`: טיפול חדש ב-`action === 'expand-name'` — מטגל class `expanded` על אלמנט השם שנלחץ, בלי לקרוא ל-`render()` (מצב UI זמני בלבד, לא נשמר ב-state).
- `style.css`: `.item-name.expanded { white-space:normal; overflow:visible; text-overflow:clip; }` — מבטל את חיתוך ה-ellipsis ומאפשר גלישת שורות.

נבדק ב-Playwright: פריט עם שם ארוך (חתוך, `scrollWidth 523 > clientWidth 220`) — לחיצה על השם מוסיפה `expanded` והטקסט נגלה במלואו בלי לפתוח את `#itemModal`; לחיצה נוספת מכווצת בחזרה; לחיצה על אזור אחר בכרטיס (שורת מחיר/כמות) ממשיכה לפתוח את מסך העריכה כרגיל, כצפוי. הרצת חבילת הרגרסיה המלאה — **0 שגיאות**. מוזג ל-`main`.

## שלב 41: סרגל "הצג סכום" צמוד לחלוטין לרצועת התחתית (בלי פריט שמבצבץ)
המשתמש שלח צילום מסך שבו סרגל הסיכום (שולם/נותר/סה"כ + "סיום רשימה") לא צמוד לגמרי לרצועת התחתית (tab-bar) — נראה פער, ופריט מהרשימה עלול "לבצבץ" ביניהם בזמן גלילה.
- **אבחון**: `#listTotalsBar` הוא `position:sticky;bottom:0` בתוך `.page`, ול-`.page` היה `padding-bottom:16px` כללי (אחרי כל התוכן, כולל אחרי הסרגל הדביק עצמו) — מכיוון ש-sticky נצמד ל-padding edge של אזור הגלילה, הסרגל "נדבק" 16px מעל התחתית האמיתית במקום ממש עליה. בנוסף, לסרגל לא היה `z-index` מפורש (בניגוד ל-`.pill-row` שכבר קיבל אחד בשלב 37 לאותה בעיה בדיוק), מה שאיפשר לכרטיס פריט "להצטייר" מעליו בזמן גלילה.
- `style.css`: הועבר ה-padding-bottom (16px) מ-`.page` (משפיע גם על הסרגל הדביק) אל `.items-container` עצמו (`padding:14px 16px 12px→16px`) — כך שהוא רלוונטי רק לרשימת הפריטים, לא לסרגל. `.page{padding:0 0 16px}` → `.page{padding:0}` (משפיע רק על `#pageLists` בפועל — ל-`#pageSummary`/`#pageStats` יש override נפרד שלא נגעתי בו). נוסף `z-index:5` ל-`.list-totals-bar` (זהה ל-`.pill-row`) כדי להבטיח שהוא תמיד מצויר מעל כרטיסי הפריטים.

נבדק ב-Playwright: רשימה עם 20 פריטים, פתיחת "הצג סכום" — המרחק בין תחתית הסרגל לראש ה-tab-bar הוא **0px בדיוק**, בכל מצבי גלילה (במנוחה, אחרי גלילה ל-400px, בחזרה למעלה, וגלילה עד הסוף) — צילום מסך אישר ויזואלית שאין שום פער/פריט מבצבץ. אומת גם שכש-הסרגל **סגור**, נשאר מרווח נשימה תקין (~16px) בין הפריט האחרון ל-tab-bar (לא נהרס). הרצת חבילת הרגרסיה המלאה — **0 שגיאות**. מוזג ל-`main`.

## שלב 42: הטוסט "נמחק" מעוצב בדיוק כמו כרטיס הפריט
המשתמש ביקש שהטוסט "נמחק" (שכבר מוצג במיקום המדויק של הפריט שנמחק — שלב 39) יהיה גם **באותה צורה** של כרטיס פריט — לא רק אותו מיקום, אלא אותו עיצוב ויזואלי.
- `js/ui/modals.js`: `showToast()` קיבל שני פרמטרים אופציונליים חדשים: `itemShaped` ו-`accentColor`. כשמועברים, מתווספת/מוסרת class `toast-item-shape` על `#toastBar`, ומוגדר/מנוקה custom property `--cat` (אותו מנגנון שבו `.item-card` כבר משתמש לפס הצבע לפי קטגוריה) — כל שאר הקריאות הקיימות ל-`showToast` (העתקה ללוח, שיתוף וכו') ממשיכות להתאפס נכון לברירת המחדל.
- `style.css`: `.toast-bar.toast-item-shape` — `border-radius:25px`, `background:var(--color-surface)`, `box-shadow:var(--shadow-sm)`, `border-inline-start:4px solid var(--cat,transparent)`, `padding:13px 14px` — זהה 1:1 ל-`.item-card`.
- `js/features/item-crud.js`: קריאת המחיקה מעבירה `itemShaped:true` ו-`accentColor: CATEGORIES[snapshot.category]` (אותו חישוב צבע שכבר קיים ב-`render.js` לכרטיסים).

נבדק ב-Playwright: הושוו הסטיילים המחושבים (computed style) של כרטיס פריט אמיתי מול הטוסט שמוצג במקומו — `border-radius`, `background`, `box-shadow`, וצבע/עובי הפס הקטגורי זהים לחלוטין. צילום מסך אישר ויזואלית שהטוסט נראה בדיוק כמו כרטיס פריט. אומת גם שטוסטים רגילים (לא של מחיקת פריט) לא נושאים את ה-class/הצבע בטעות. הרצת חבילת הרגרסיה המלאה — **0 שגיאות**. מוזג ל-`main`.

## שלב 43: צבע אדום ייעודי לטוסט "נמחק" (במקום לבן כמו כרטיס רגיל)
בשלב 42 עוצב הטוסט "נמחק" להיראות בדיוק כמו כרטיס פריט רגיל (רקע לבן). המשתמש העיר שבדיוק בגלל זה לא ברור מיד שבוצעה מחיקה — ביקש צבע שונה שיבליט את זה.
- `style.css`: `.toast-bar.toast-item-shape` — הרקע הוחלף מלבן (`var(--color-surface)`) לאדום עדין `rgba(239,68,68,.08)` + border אדום `1.5px solid rgba(239,68,68,.25)` — **בדיוק** אותה קונבנציה שכבר קיימת ב-`.pill-red` (כפתור "מחק"), כדי להישאר בשפת העיצוב הקיימת ולא להמציא צבע חדש. פס הצבע לפי קטגוריה (`border-inline-start: var(--cat)`) הוחלף בפס אדום אחיד (`var(--color-danger)`) — כבר לא רלוונטי לציין קטגוריה על התראת מחיקה. טקסט התוכן (שם הפריט + "נמחק") מקבל `color: var(--color-danger)`, בעוד "בטל" נשאר בצבע ה-accent הסגול הקיים — כך המחיקה (אדום) והפעולה האפשרית (סגול) מובחנות בבירור זו מזו.
- הצורה/מיקום/גודל מהשלבים 39/42 לא נגעו — עדיין `border-radius:25px`, עדיין מוצג בדיוק במקום הפריט שנמחק.

נבדק ב-Playwright: הסטיילים המחושבים של הטוסט אחרי מחיקה — רקע אדום עדין, border אדום, טקסט "נמחק" אדום, "בטל" סגול. צילום מסך אישר ויזואלית שהמחיקה ברורה ובולטת. אומת שטוסטים רגילים (שיתוף/העתקה וכו') לא מושפעים. הרצת חבילת הרגרסיה המלאה — **0 שגיאות**. מוזג ל-`main`.

## שלב 44: תיקון קריאות טוסט המחיקה — רקע אדום מלא במקום שקוף חלקית
המשתמש שלח צילום מסך שבו טקסט הטוסט וטקסט פריט שמתחתיו **חופפים ובלתי קריאים**. אבחון: רקע הטוסט משלב 43 היה `rgba(239,68,68,.08)` — שקיפות של 8% בלבד. אחרי מחיקת פריט, הרשימה נגללת מחדש (render()) והפריט הבא "גולש" למקום המדויק שבו יושב הטוסט (אותו anchorRect משלב 39) — ומכיוון שהרקע כמעט שקוף, הפריט שמתחתיו "מבצבץ" דרכו ומתערבב עם טקסט הטוסט.
- `style.css`: `.toast-bar.toast-item-shape` — הרקע הוחלף לאדום **מלא ואטום**: `#dc2626` (גוון אדום כהה יותר שכבר קיים בקוד כ-hover state של `.btn-danger`, נבחר על פני `--color-danger`/`#ef4444` הבהיר יותר כדי לתת ניגודיות טובה יותר מול טקסט לבן). הוסר ה-border/border-inline-start הקודמים (מיותרים על רקע אטום מלא). `.toast-content` בתוך הטוסט מקבל `color:#fff`, ו-`.toast-undo-btn` מקבל `color:#fff` + `font-weight:700` + `text-decoration:underline` כדי שה"בטל" עדיין יבלוט כפעולה נפרדת וברורה על הרקע האדום (במקום הסגול הקודם, שהיה מאבד ניגודיות על אדום).

נבדק ב-Playwright: שוחזר בדיוק מצב הבאג (רשימה עם 3 פריטים באותה קטגוריה, מחיקת האמצעי כך שהפריט הבא גולש בדיוק למקום הטוסט) — הרקע עכשיו אטום מלא (`rgb(220,38,38)`), טקסט לבן קריא לגמרי, "בטל" קו תחתי לבן וברור, ואין שום חפיפה/בצבוץ של הפריט שמתחתיו (צילום מסך אישר ויזואלית). הרצת חבילת הרגרסיה המלאה — **0 שגיאות**. מוזג ל-`main`.

## שלב 45: שיתוף רשימה חי בין כמה משתמשים (Supabase Realtime)
המשתמש ביקש פיצ'ר שיתוף אמיתי בזמן אמת — 2+ אנשים רואים ומעדכנים את אותה רשימה אונליין. זה דורש backend מרוחק, בניגוד להחלטת ההיקף המקורית "ללא שרת". לאחר דיון (הוסבר שאין דרך אמיתית לסנכרון חי בלי backend כלשהו, וש"שיטת השיתוף" — אימייל/טלפון/ברקוד — היא רק ערוץ מסירה לקישור, לא מנגנון הסנכרון עצמו) — **הוחלט על Supabase** (חלופה בקוד-פתוח ל-Firebase, אותו מודל: SDK צד-לקוח בלבד, הגנה דרך Row Level Security ולא הסתרת מפתחות).

המשתמש חיבר Connector של Supabase לשיחה, מה שאיפשר ביצוע כל ההקמה ישירות דרך כלי MCP (בלי צורך להעתיק ידנית URL/מפתחות): שימוש בפרויקט Supabase קיים וריק בחשבון (`ronmailx-boop's Project`, id `ftvmhhbacepwcxigvpdc`).

**סכמה + אבטחה** (`apply_migration`): טבלת `shared_lists` (`id uuid pk default gen_random_uuid()`, `data jsonb`, `updated_at timestamptz`), RLS מופעל עם policies ל-select/insert/update ל-role `anon` עם `using(true)` — מודל אבטחה "מי שיש לו את ה-UUID (הקישור) יכול לגשת", כמו Google Docs "כל מי שיש לו את הקישור". **הערת אבטחה מתועדת בכוונה**: `using(true)` נדרש כדי ש-Realtime (postgres_changes) יעבוד בכלל עבור לקוחות אנונימיים (RLS חוסם גם broadcast של Realtime, לא רק REST); המשמעות: מבחינה טכנית אפשר לשלוף את **כל** הרשימות המשותפות דרך קריאת REST גולמית בלי לסנן לפי id (לא ניתן להצגה/גילוי דרך ה-UI של האפליקציה, אבל לא מוצפן קריפטוגרפית). הוחלט שזה תואם את המודל וההיקף (רשימות קניות משפחתיות, לא מידע רגיש) ועדיף על פני הפתרון המאובטח יותר (RPC + הצפנה מקצה-לקצה) שהיה מסבך משמעותית פיצ'ר שנדרש "הכי פשוט שאפשר". Realtime הופעל (`alter publication supabase_realtime add table`). אומת ב-SQL ישירות (`set local role anon`) ש-insert/select-by-id/update עובדים נכון תחת ה-RLS.
- `js/core/supabase-config.js` (חדש): `SUPABASE_URL` + `SUPABASE_ANON_KEY` (מפתח publishable, גלוי בכוונה בקוד לקוח — מודל זהה למפתחות Firebase Web SDK הקיים כבר בכללי הפרויקט).
- `js/features/collab.js` (חדש): `enableSharing(listId)` (יוצר `shareId`, INSERT ל-Supabase, נרשם ל-subscription), `getShareLink(shareId)`, `joinSharedList(shareId)` (נקרא כש-`?share=` מופיע ב-URL — שולף נתונים, יוצר/מעדכן רשימה מקומית עם אותו `shareId`). ה-SDK (`@supabase/supabase-js@2`) נטען דרך `import()` דינמי מ-CDN (jsdelivr) — **רק** כשבאמת נדרש (יש רשימה משותפת פעילה או `?share=` ב-URL), לא נטען למשתמשים שלא משתמשים בפיצ'ר. כל קריאות הרשת עטופות ב-try/catch עם הודעת שגיאה ברורה בעברית (`נגישות רשת נכשלה — בדקו חיבור ונסו שוב`) — כולל תיקון באג אמיתי שנמצא תוך כדי בדיקה: כשל ב-import הדינמי (חסום ב-sandbox זה) לא היה נתפס במקור, מה שהשאיר את כפתור "הפוך לרשימה משותפת" תקוע במצב disabled לצמיתות בלי שום הודעה למשתמש.
- `js/ui/render.js`: נוספה תשתית hooks כללית — `onRender(fn)` רושם פונקציה שתופעל בסוף כל `render()`. משמש את collab.js לדחוף (debounced, 600ms) את תוכן הרשימה הנוכחית ל-Supabase בכל פעם שיש לה `shareId`, בלי לגעת בעשרות מקומות הקריאה הקיימים ל-`render()` באפליקציה.
- `js/core/store.js`: `makeListId` הפך מ-`function` פנימית ל-`export function` (נדרש ל-collab.js ליצירת רשומת רשימה מקומית חדשה כשמצטרפים לרשימה משותפת).
- `index.html`: פיל חדש `#liveShareBtn` (אייקון `ph-users-three`) ליד כפתור השיתוף הקיים; מודל חדש `#liveShareModal` — לחיצה על "הפוך לרשימה משותפת" יוצרת קישור שיתוף, ואז מוצג קישור (input לקריאה בלבד, `focus` בוחר הכל) + כפתור "שתף קישור" (`navigator.share()` הקיים, עם נפילה חזרה להעתקה ללוח).
- `sw.js`: `CACHE_NAME` הועלה ל-`vplus-v7`, `collab.js`/`supabase-config.js` נוספו ל-precache.

**מודל נתונים**: שיתוף הוא **opt-in לכל רשימה בנפרד** — שדה `shareId` אופציונלי על `list`. רשימה בלי `shareId` נשארת מקומית לגמרי, בלי שום שינוי התנהגות. תוכן שמגיע מרחוק עובר דרך אותו נתיב רינדור/סניטציה קיים (`itemCardHTML`, `sanitize()`) — אין משטח XSS חדש.

**QR code נדחה לסבב המשך**: תוכנן כערוץ שיתוף פנים-אל-פנים נוסף, אבל נדחה כדי לא למהר עם ספריית CDN שלא ניתנת לאימות בסביבת ה-sandbox הזו (חסימת CDN מוכרת) — הליבה (קישור שיתוף, כבר עובר גם דרך `navigator.share()` הקיים לוואטסאפ/אימייל/SMS) שלמה ועובדת בלעדיו.

נבדק: (1) ישירות מול הפרויקט האמיתי ב-Supabase דרך `execute_sql` עם `set local role anon` — insert/select-by-id/update עובדים נכון תחת ה-RLS. (2) ב-Playwright עם client מדומה (הזרקת מודול מזויף במקום ה-import החסום מ-CDN) — זרימת enableSharing (יצירת קישור, subscribe), joinSharedList (קריאת `?share=`, טעינת רשימה מרוחוק, עדכון UI), וה-hook האוטומטי שדוחף עדכון ל-Supabase אחרי כל שינוי מקומי ברשימה משותפת — כולם עובדים כצפוי. (3) תוקן ואומת טיפול שגיאות רשת (טוסט ברור בעברית, כפתור לא נתקע). **מגבלת בדיקה מוצהרת**: אימות end-to-end אמיתי בין שני מכשירים/דפדפנים אמיתיים לא בוצע כאן (חסום ע"י מדיניות הרשת של ה-sandbox) — יידרש בדיקה של המשתמש בפועל אחרי הפריסה. הרצת חבילת הרגרסיה המלאה (רשימות רגילות לא-משותפות) — **0 שגיאות**.

## שלב 46: תיקון סנכרון חד-כיווני בשיתוף רשימה חי
המשתמש בדק בפועל עם שני מכשירים: כשהחבר (B) הוסיף פריט, המשתמש (A) ראה אותו — אבל כש-A הוסיף פריט, B לא ראה. סנכרון עבד בכיוון אחד בלבד.

**אבחון**: קוד ה-push (`pushCurrentListIfShared`) וקוד ה-subscribe (`subscribeToList`, WebSocket דרך `postgres_changes`) זהים אצל שני הצדדים — אין הבדל לוגי בין "יוצר השיתוף" ל"מצטרף". המסקנה: הבעיה היא אמינות החיבור, לא לוגיקה. חיבורי WebSocket נשברים בקלות במובייל (מסך נעול / מעבר אפליקציות / רקע) בלי שהקוד הקודם ניסה להתחבר מחדש בכלל. אם ה-subscription של B מת בשקט, דחיפות מ-A (HTTP `update()` רגיל, תמיד עובד) לא היו מגיעות ל-B כי B לא היה "מאזין" בפועל — בעוד ש-B בכל זאת יכול לדחוף (ולכן A, שה-WebSocket שלו כן היה חי, ראה את מה ש-B שלח). זה בדיוק מסביר את חוסר-הסימטריה שדווח.

**הפתרון: שכבת אמינות נוספת, לא רק "תיקון"** — `js/features/collab.js`:
- **Polling fallback**: `startPolling(shareId)`/`stopPolling()` — `setInterval` כל 4 שניות ששולף `data,updated_at` מ-Supabase ומשווה ל-`lastKnownUpdatedAt` הידוע מקומית; אם השתנה, מעדכן ומרנדר. **לא תלוי בכלל** בכך שה-WebSocket עובד — רשת אמינה (HTTP), לא WebSocket עדין. מופעל תמיד יחד עם ה-subscription (`subscribeToList` קורא גם ל-`startPolling`), לא כתחליף לה — WebSocket עדיין נותן עדכון כמעט-מיידי כשהוא עובד, polling מבטיח שזה יגיע גם כשהוא לא עובד.
- **התחברות מחדש ב-`visibilitychange`**: כשהטאב חוזר לפעיל (`document.visibilityState === 'visible'`), נקרא שוב ל-`subscribeToList` לרשימה הנוכחית אם היא משותפת — תופס בדיוק את המקרה של WebSocket שנסגר ברקע.
- `applyRemoteUpdate()` — פונקציה משותפת אחת שמעדכנת `db`/`lastKnownUpdatedAt`/`render()`, בשימוש גם מה-WebSocket וגם מה-polling, כדי לא לשכפל לוגיקה.
- `enableSharing`/`joinSharedList` מבקשים גם `updated_at` בתגובה (לא רק `id`/`data`) כדי לזרוע את `lastKnownUpdatedAt` הנכון מההתחלה, ו-`pushCurrentListIfShared` מעדכן אותו אחרי push מוצלח — מונע רינדור מיותר על הד עצמי (echo) מה-polling.
- `sw.js`: `CACHE_NAME` הועלה ל-`vplus-v8` (השינוי הלוגי ב-collab.js דורש רענון cache).

נבדק ב-Playwright: תרחיש עם client מדומה שבו ה-WebSocket "מת" לגמרי (`.on()`/`.subscribe()` לא שולחים שום אירוע לעולם) — סימולציה של עדכון מ"מכשיר אחר" (מוטציה ישירה על מצב ה-remote המדומה, בלי לעבור דרך ה-client של הדף הזה בכלל, בדיוק כמו מכשיר שני אמיתי) — אומת שה-polling עדיין מביא את הפריט החדש תוך כ-4 שניות, בלי תלות ב-WebSocket בכלל. אומת גם שלרשימה רגילה (לא-משותפת) אין שום קריאת רשת ל-Supabase SDK בכלל (0 עלות ביצועים/סוללה). הרצת חבילת הרגרסיה המלאה — **0 שגיאות**. מוזג ל-`main`.

**מגבלת בדיקה מוצהרת (כרגיל)**: אימות מלא בין שני מכשירים אמיתיים במצבי רקע/נעילת מסך אמיתיים לא בוצע כאן — יידרש בדיקה של המשתמש בפועל.

## שלב 47: שמירת פרויקט Supabase ער — GitHub Actions במקום Claude Routine
המשתמש שאל אם יעבור שבוע בלי שימוש בקישור השיתוף, האם הפרויקט של Supabase (בתוכנית החינמית) יושהה אוטומטית — התשובה: כן, אחרי כ-7 ימים ללא פעילות. הוצע ואושר פתרון: פינג תקופתי אוטומטי שמונע את ההשהיה.

**ניסיון ראשון (נכשל)**: יצירת Routine מתוזמן דרך `create_trigger` (כלי claude-code-remote) שירוץ בתוך שיחת ה-Claude הזו כל 3 ימים ויריץ שאילתת SQL טריוויאלית דרך ה-MCP connector של Supabase. הניסיון הראשון נוצר בלי גישת connectors בכלל (אזהרה מפורשת מהכלי). ניסיון תיקון עם פרמטר `connectors:["Supabase"]` **נדחה על ידי המערכת**: "the connectors parameter is not available for this organization" — כלומר אין דרך להעניק ל-Routine מתוזמן גישה לכלי MCP בארגון הזה בכלל. ה-Routine השבור נמחק (`delete_trigger`).

**הפתרון בפועל**: `.github/workflows/keep-supabase-alive.yml` (חדש) — GitHub Actions workflow מתוזמן (`cron: "0 9 */3 * *"`, כל 3 ימים, בתוך מרווח הביטחון של 7 הימים) ששולח בקשת `GET` קטנה וזניחה ל-REST API הציבורי של Supabase (`/rest/v1/shared_lists?select=id&limit=1`) עם מפתח ה-anon/publishable הציבורי (אותו מפתח שכבר גלוי בקוד הלקוח — לא סוד, לא Secret חדש נדרש). זו תעבורת רשת **חיצונית אמיתית** אל ה-API של Supabase (בניגוד לשאילתת SQL פנימית שהייתה רצה רק אם היה חיבור MCP) — סביר שנחשבת "פעילות" אמיתית מבחינת המדיניות של Supabase. `workflow_dispatch` מאפשר גם הפעלה ידנית לבדיקה. פתרון עדיף בפועל: לא תלוי בזמינות MCP connectors/מדיניות ארגון, רץ על תשתית GitHub Actions הקיימת של הפרויקט (חינמי, שימוש זניח בדקות ריצה).

## שלב 48: תיקון "פריט נעלם" בשיתוף חי — עדכונים ישנים דרסו נתונים טריים
המשתמש דיווח: בהוספת פריט לרשימה משותפת, הפריט מופיע לרגע ואז **נעלם בשני הדפדפנים**.

**אבחון**: `applyRemoteUpdate` (ב-`js/features/collab.js`, גם ב-WebSocket וגם ב-polling) הייתה מיישמת כל עדכון נכנס בלי לבדוק אם הוא **באמת חדש יותר** מהמצב הידוע מקומית — רק בדקה "האם הוא שונה" (`!==`), לא "האם הוא מאוחר יותר". זה פתח שני נתיבי באג:
1. **הד עצמי (self-echo)**: אחרי push מוצלח, ה-subscription של אותו דפדפן מקבל בחזרה את העדכון שהוא עצמו שלח — `render()` שמתבצע כתוצאה מזה מפעיל שוב את `onRender`, שמתזמן push **נוסף** של אותם נתונים (בזבזני, לולאה מתמשכת).
2. **תגובת polling שמגיעה באיחור (out-of-order)**: אם בקשת ה-poll הקודמת ("מה יש בשרת?") נשלחה **לפני** שהפריט נוסף, אבל התשובה שלה חוזרת **אחרי** שהפריט כבר נוסף ונדחף בהצלחה — הקוד ראה `updated_at` שונה מהידוע (כי בינתיים כבר התעדכן) והחיל את הנתונים **הישנים** (בלי הפריט) בחזרה — מוחק את הפריט מהתצוגה, ואז גם דוחף את הגרסה חסרת-הפריט בחזרה לשרת (אובדן נתונים אמיתי, לא רק ויזואלי).

**התיקון**: `applyRemoteUpdate` משווה עכשיו זמנים בפועל — `new Date(remoteUpdatedAt) <= new Date(lastKnownUpdatedAt)` → מתעלם (עדכון ישן/כפול/הד עצמי), ומיישם רק עדכון **חדש ממש**. זה פותר גם את בעיית ה-self-echo (הד יש לו אותו timestamp בדיוק שכבר נרשם) וגם את בעיית ה-out-of-order polling (תשובה מאוחרת עם timestamp ישן מתעלמת ממנה אוטומטית) — באותו תיקון אחד.

נבדק ב-Playwright (client מדומה שמאפשר להזריק אירועי "הד" ו"עדכון ישן" ידנית): (1) הד עצמי אחרי push אמיתי — הפריט נשאר, אין קפיצה. (2) אירוע עם `updated_at` ישן יותר וללא הפריט — מתעלם, הפריט נשאר. (3) אירוע עם `updated_at` חדש יותר באמת (מהצד השני) — כן מתעדכן כראוי. הרצת חבילת הרגרסיה המלאה — **0 שגיאות**. `sw.js`: `CACHE_NAME` הועלה ל-`vplus-v9`.

## שלב 49: הצגת מספר גרסה באפליקציה
המשתמש ביקש מספר גרסה גלוי באפליקציה שעולה בכל שינוי, כדי לדעת שהוא רואה את הגרסה העדכנית בלי לנקות מטמון ידנית.
- **מקור האמת**: מספרי ה"שלב" ב-`PROJECT_STATE.md` עצמם (כבר גדלים ב-1 בעקביות בכל שינוי) — לא נוצר מונה נפרד לתחזוקה.
- `js/core/constants.js`: `export const APP_VERSION = 49;`.
- `index.html`: `<div id="appVersion" class="app-version"></div>` נוסף בתוך `.app-logo`, מתחת ללוגו "V plus PRO".
- `js/app.js`: מייבא `APP_VERSION`, ממלא את הטקסט (`'גרסה ' + APP_VERSION`) בזמן האתחול.
- `style.css`: `.app-version` — טקסט קטן ואפור עדין (`0.5625rem`, `color:var(--color-neutral-400)`), לא בולט מדי, רק לצורך אימות.
- **תהליך עבודה קדימה**: מכאן והלאה, בכל שינוי עתידי, `APP_VERSION` יעודכן יחד עם מספר ה"שלב" ב-`PROJECT_STATE.md` באותו commit — כך שהמספר המוצג תמיד ישקף את הגרסה האחרונה שנפרסה בפועל.
- `sw.js`: `CACHE_NAME` הועלה ל-`vplus-v10`.

נבדק ב-Playwright: הטקסט "גרסה 49" מוצג נכון בהדר; צילום מסך אישר ויזואלית מיקום וגודל מתאימים (לא מפריע ללוגו). הרצת חבילת הרגרסיה המלאה — **0 שגיאות**. מוזג ל-`main`.

## שלב 50: תיקון "לפעמים הוספת מוצר לא עובדת" — מיזוג ברמת הפריט במקום החלפה מלאה
המשתמש דיווח: תוך כדי בדיקה עם 2 משתמשים, לפעמים לוחצים "שמירה" על מוצר חדש ושום דבר לא מופיע — בלי קשר לאיזה משתמש.

**אבחון**: המשך ישיר לבאג משלב 48, אך נגרם עכשיו מ**פעילות מקבילה אמיתית** של שני משתמשים, לא רק הד-עצמי: משתמש A מוסיף פריט מקומית (push מתוזמן ב-600ms debounce) — ולפני שה-push מספיק להישלח, מגיע עדכון **אמיתי וחדש-אמת** מהשרת (כי משתמש B עשה שינוי אחר, למשל וי על פריט, **באותו חלון זמן**). תיקון שלב 48 (בדיקת timestamp) מרשה ליישם את זה **בצדק** — הוא באמת חדש יותר. הבעיה: `applyRemoteUpdate` **החליפה את כל מערך הפריטים** בגרסת השרת — שאין בה מושג על הפריט החדש של A (B לא ידע עליו) — כך שהפריט של A נמחק, ואז ה-`render()` הנגרם מזה מפעיל push חדש (מבטל את זה שהכיל את הפריט) — הפריט אובד גם מקומית וגם בשרת.

**הפתרון**: מיזוג ברמת הפריט הבודד (לפי `id`) במקום החלפה מלאה של מערך הפריטים. `js/features/collab.js`: פונקציה חדשה `mergeItems(localItems, remoteItems)` — מתחילה מפריטי השרת (מקור אמת לשינויים ששני הצדדים כבר מכירים), ומוסיפה בחזרה כל פריט שקיים **רק מקומית** (נוסף אך טרם הספיק להידחף) — כך שהוא לא נעלם. `applyRemoteUpdate` משתמשת בזה במקום `db.lists[listId] = {...remoteData, shareId}` ישירות.

**מגבלה שנשארת, מוצהרת במודע**: אם המשתמש השני מוחק פריט בדיוק באותו רגע שהמכשיר שלנו עדיין "לא יודע" על המחיקה, המיזוג עלול "להחזיר" את הפריט הזה זמנית (עד מחזור הסנכרון הבא). הוחלט להעדיף "לא לאבד הוספות" (הבעיה שדווחה בפועל) על פני "לא להחזיר זמנית מחיקות" (תרחיש נדיר יותר, לא דווח).

נבדק ב-Playwright: שוחזר בדיוק התרחיש שדווח — הוספת פריט מקומי, ומיד אחריו עדכון מרוחק חדש-אמת (וי על פריט קיים אחר) שאין בו מושג על הפריט החדש — אומת שהפריט החדש **נשאר** (לא נעלם), וגם שהעדכון האמיתי מהצד השני (הוי) כן מוחל נכון. הרצת כל בדיקות ה-collab הקודמות (polling fallback, race/echo/stale/newer, full flow) + חבילת הרגרסיה המלאה — **0 שגיאות**. `sw.js`: `CACHE_NAME` הועלה ל-`vplus-v11`. `APP_VERSION` עודכן ל-50.

## שלב 51: אינדיקטור "מישהו מזין פריט" בשיתוף חי (Broadcast)
המשתמש שאל: כשמשתמש אחד ברשימה משותפת מתחיל להזין פריט חדש, האם אפשר שהמשתמש השני יראה שמישהו מקליד עכשיו?

**הגישה**: Supabase Realtime **Broadcast** — לא Presence ולא טבלה נוספת. Broadcast הוא "ping" חולף שלא נשמר בשום מקום, בדיוק מתאים ל"מישהו מקליד עכשיו". זה משתמש **באותו channel** שכבר פתוח (`shared_lists:<shareId>`) לצורך ה-`postgres_changes` הקיים — לא נדרש חיבור/טבלה/RLS נוספים. Broadcast לא חוזר אל השולח עצמו כברירת מחדל (`self:false`) — אין סיכון להד-עצמי כמו שהיה עם postgres_changes (שלב 48).

**מגבלה מוצהרת**: בניגוד לסנכרון הנתונים עצמו, לתכונה הזו **אין polling fallback** — היא תלויה לגמרי בכך שה-WebSocket חי. זה מקובל כי זו תכונה קוסמטית בלבד (nice-to-have); אם ה-WebSocket מת, האינדיקטור פשוט לא יופיע לפעם הזו — הנתונים עצמם עדיין מוגנים ע"י ה-polling הקיים (שלא תלוי בזה).

**שינויים ב-`js/features/collab.js`**:
- `sendTypingPing()` — נשלחת כשמשתמש מקליד ב-`#itemName`, רק אם הרשימה הנוכחית משותפת (`shareId`) ויש `channel` פעיל. עם throttle (מקסימום פעם ב-2 שניות) כדי לא להציף בזמן הקלדה רציפה. עטופה ב-try/catch (שליחה שנכשלת = פשוט האינדיקטור לא יופיע הפעם, לא באג קריטי).
- `subscribeToList()` — נוסף מאזין `.on('broadcast', {event:'typing'}, ...)` על אותו channel, קורא ל-`showTypingIndicator()`.
- `showTypingIndicator()` — מציגה את `#typingIndicator`, ומאתחלת טיימר הסתרה אוטומטי (3.5 שניות מאז ה-ping האחרון) — אם אין ping נוסף, האינדיקטור נעלם לבד.
- `initCollab()` — מאזין `input` חדש על `#itemName` שקורא ל-`sendTypingPing()`.

**שינויים נוספים**: `index.html` — `<div id="typingIndicator" class="typing-indicator hidden" aria-live="polite">` בין `#budgetWarning` ל-`#itemsContainer` (ה-`aria-live` מוצדק — הודעת סטטוס דינמית אמיתית). `style.css` — קלאס `.typing-indicator` (פיל סגול עדין, נבדל מ-`.budget-warning`).

נבדק ב-Playwright עם fake-client מורחב שתומך ב-`channel.send`/`channel.on('broadcast',...)` וחושף hook (`window.__fireBroadcastTyping`): (1) הקלדה ברשימה **לא**-משותפת לא שולחת broadcast בכלל. (2) הקלדה ברשימה משותפת שולחת אירוע `broadcast:typing`. (3) קבלת broadcast מציגה את האינדיקטור, ואחרי כ-4 שניות בלי ping נוסף הוא נעלם אוטומטית. הרצת כל בדיקות ה-collab הקודמות (race/echo/stale/merge וכו') + חבילת הרגרסיה המלאה — **0 שגיאות**. `sw.js`: `CACHE_NAME` הועלה ל-`vplus-v12`. `APP_VERSION` עודכן ל-51.

## שלב 52: תיקון אובדן **קבוע** של מוצר כששני משתמשים מוסיפים בו-זמנית
המשתמש דיווח: שני המשתמשים ניסו להוסיף מוצר בו-זמנית (כל אחד מוצר שונה) — ואחד המוצרים נעלם **לצמיתות**, גם אחרי רענון/המתנה (חמור יותר מהמגבלה הזמנית שתועדה בשלב 50).

**אבחון**: ב-`applyRemoteUpdate`, הבדיקה `remoteUpdatedAt <= lastKnownUpdatedAt → להתעלם` הניחה ש"מבוגר יותר בזמן = בטוח להתעלם" — הנחה נכונה רק לעדכונים **רציפים**, אבל שגויה בכתיבה מקבילה אמיתית: A מוסיף X ודוחף ב-tA; B מוסיף Y (בלי לדעת על X) ודוחף ב-tB מעט אחרי tA — כך ש-`lastKnownUpdatedAt` אצל B כבר קפץ ל-tB. כשמגיע אצל B העדכון של A (tA, מכיל את X) — הוא "מבוגר" מ-tB הידוע ל-B, ולכן **נזרק לגמרי** לפי הבדיקה הישנה — אבל X מעולם לא היה חלק ממה שב-B כבר ידע, כך שההתעלמות הזו לא דילגה על כפילות, היא זרקה מידע ייחודי לצמיתות (וגם דחיפת B לשרת, שלא כללה את X מלכתחילה, דרסה שם את מה ש-A כתב).

**התיקון**: הפרדה בין שתי מטרות שהיו מעורבבות בבדיקה אחת:
1. **הד-עצמי מדויק בלבד**: מתעלמים רק כש-`remoteUpdatedAt` **זהה בדיוק** ל-`lastKnownUpdatedAt` (לא "קטן-שווה") — זה הד אמיתי של הדחיפה שלנו, לא ענף מקביל.
2. **מיזוג תמיד**: בכל מקרה אחר (כולל timestamp "ישן"), `mergeItems` (איחוד לפי id, משלב 50) מופעל תמיד — איחוד רק **מוסיף**, לעולם לא מוחק פריט מקומי קיים, ולכן בטוח גם כשמגיע עדכון "ישן-אמת" עם מידע חדש.
- נוספה `sameListContent()` — משווה את הרשימה הממוזגת לקיימת (שם/תקציב/קישור + קבוצת פריטים) כדי **לא** לשמור/לרנדר/לתזמן דחיפה מיותרת כשהמיזוג לא הוסיף בפועל שום דבר חדש — מונע פינג-פונג אינסופי של דחיפות בין שני הצדדים שהיה עלול להיווצר עכשיו שכל עדכון (גם "ישן") מעובד.
- `lastKnownUpdatedAt` מתעדכן רק כשה-timestamp החדש מאוחר יותר בפועל (לא נסוג אחורה).

נבדק ב-Playwright: שוחזר בדיוק התרחיש שדווח — B מוסיף ודוחף (timestamp מאוחר), ורק **אחר כך** מגיע עדכון עם timestamp **מוקדם יותר** שמכיל פריט של A שB מעולם לא ראה → **X נשמר** (התיקון עובד; לפני התיקון X היה נעלם). אומת שאין פינג-פונג דחיפות (רק 2 קריאות `update()` בסך הכול — הדחיפה העצמית + דחיפת ריפוי אחת). כל בדיקות ה-collab הקודמות (race/echo/merge/polling/typing) עדיין עוברות + חבילת הרגרסיה המלאה — **0 שגיאות**. `sw.js`: `CACHE_NAME` הועלה ל-`vplus-v13`. `APP_VERSION` עודכן ל-52.

## שלב 53: תיקון קריסה מוחלטת של השיתוף החי — רגרסיה משלב 52
מיד אחרי שלב 52 המשתמש דיווח: **כלום לא עובד** — לא חיווי הקלדה ולא סנכרון פריטים בכלל, בלי הודעת שגיאה. כל משתמש מצליח להוסיף רק לעצמו. אימתתי קודם (דרך היסטוריית ה-GitHub Actions של `keep-supabase-alive.yml` — ריצה מוצלחת מאותו יום, "Supabase responded with HTTP 200") שהפרויקט עצמו חי ונגיש — הבעיה בקוד הלקוח, לא בשרת.

**אבחון**: שני מנגנוני ה"הגנה מפני לולאה" שנוספו בשלב 52 השוו ערכים **שעברו דרך Postgres** בעזרת השוואת מחרוזות/`JSON.stringify` נאיבית — לא בטוח, כי Postgres לא מבטיח לשמר פורמט/סדר מקוריים:
1. **זיהוי הד-עצמי** (`isSelfEcho`) השווה `remoteUpdatedAt === lastKnownUpdatedAt` כמחרוזות. העמודה `updated_at` היא `timestamptz` — כשהיא חוזרת מ-Postgres/PostgREST הפורמט שונה מה-ISO string שיצרנו ב-JS (`...+00:00` לעומת `...Z`), אותה נקודת זמן בדיוק אך מחרוזת שונה → **הד עצמי אף פעם לא זוהה בתור הד**.
2. **בדיקת שוויון תוכן** (`sameListContent`) השתמשה ב-`JSON.stringify` גולמי על הפריטים. העמודה `data` היא `jsonb` — Postgres **לא מבטיח לשמר סדר מפתחות** באובייקט (מתועד רשמית). פריט שחוזר מהשרת עם סדר מפתחות שונה מייצר מחרוזת `JSON.stringify` שונה על **אותו תוכן בדיוק** → הבדיקה קבעה "השתנה" כשלא השתנה כלום.

**השילוב ההרסני**: כל דחיפה חוזרת כהד, לא מזוהה ככזה, "בדיקת השינוי" גם נכשלת ומדווחת "כן השתנה" → `save()`+`render()` → דחיפה **נוספת** של אותו תוכן, עם timestamp חדש → חוזר על עצמו **בלי סוף**. כל מכשיר נכנס ללולאת-דחיפות עצמית אינסופית שדורסת בשרת כל מה שהמשתמש השני כותב באמצע — ומכאן "כל אחד מצליח להוסיף רק לעצמו".

**התיקון**: להשוות ערכים בצורה חסינה למעבר דרך Postgres, לא כמחרוזות גולמיות:
- **הד-עצמי**: `new Date(remoteUpdatedAt).getTime() === new Date(lastKnownUpdatedAt).getTime()` — משווה נקודת זמן בפועל, לא מחרוזת (מנרמל `Z` מול `+00:00`), עדיין רק שוויון מדויק (לא חוזר להתנהגות "קטן-שווה" ההרסנית משלבים קודמים).
- **שוויון תוכן**: `canonicalize(value)` — עוזר רקורסיבי חדש שממיין מפתחות אובייקט (ומטפל במערכים) לפני ה-`JSON.stringify`, כך שסדר מפתחות שונה מ-jsonb לא משפיע.

נבדק ב-Playwright עם fake client שמדמה **בדיוק** את התנהגות Postgres בפועל (מחזיר timestamp בפורמט `+00:00` ופריטים עם סדר מפתחות הפוך): שוחזר הבאג במפורש מול הקוד הישן (`updateCallCount` טיפס 5→8→12 בלי הפסקה — לולאת דחיפות אינסופית מוכחת), ואומת שמול הקוד המתוקן הוא נשאר יציב (1, ואז 2 אחרי מיזוג אמיתי מהצד השני). כל בדיקות ה-collab הקודמות (race/merge/polling/typing/concurrent-add) + חבילת הרגרסיה המלאה — **0 שגיאות**. `sw.js`: `CACHE_NAME` הועלה ל-`vplus-v14`. `APP_VERSION` עודכן ל-53.

## שלב 54: החלפת שיטת הסנכרון — הפסקה לגמרי מהשוואות טקסט/timestamp שבירות
אחרי שלב 53 המשתמש דיווח שוב: **עדיין כלום לא עובד**. זה הסימן שההשוואות ה"חכמות" (timestamp כמחרוזת, JSON.stringify של תוכן) פשוט **לא אמינות מספיק** מול איך ש-Postgres בפועל מחזיר נתונים — כל ניסיון לתקן אותן (שלב 53) יצר עוד אופן כשל. ההחלטה: **להחליף שיטה לגמרי** — להפסיק להשוות טקסט/JSON בכלל, ולעבור לגישה הרבה יותר פשוטה ועמידה.

**השיטה החדשה**: `applyRemoteUpdate` כבר לא מנסה "לזהות הד עצמי" או "לבדוק אם התוכן השתנה" באמצעות השוואת מחרוזות. במקום זה:
- **תמיד** ממזגת (`mergeItems`, איחוד לפי id — נשאר משלב 50) ו**תמיד** שומרת ומרנדרת — פעולות זולות ואידמפוטנטיות, אין סיבה "לחסוך" אותן.
- ההחלטה **האם לדחוף בחזרה לשרת** לא תלויה בהשוואת timestamp/תוכן כלל — רק בשאלה **מספרית פשוטה**: האם המיזוג בפועל הוסיף פריט מקומי שלא היה ברשימה שהתקבלה מהשרת (`items.length > remoteData.items.length`)? אם לא הוסיף כלום — העדכון שהתקבל כבר משקף את כל מה שאנחנו יודעים, ואין מה "לרפא" בשרת (`suppressNextPush = true`, מדלג על הדחיפה הבאה). אם כן הוסיף — השרת חסר את הפריט הזה, וצריך לדחוף בחזרה (ריפוי, כמו בשלב 50/52).
- השוואת **מספר** פריטים חסינה לגמרי מכל עיוותי טקסט של Postgres (פורמט timestamp, סדר מפתחות ב-jsonb) — לא נשארה שום השוואת מחרוזת/JSON רגישה-לפורמט בכל התהליך.
- `startPolling` הופשט בהתאם: קורא ל-`applyRemoteUpdate` בכל טיק (כל 4 שניות) בלי תנאי מוקדם על timestamp — הפונקציה עצמה כבר זולה ובטוחה גם כשלא השתנה כלום.

הוסרו לגמרי: `isSelfEcho` (השוואת מחרוזות timestamp), `sameListContent`, `canonicalize`, `sortedById` — כל הקוד ששלב 52-53 הוסיפו וניסו לתקן.

נבדק ב-Playwright: אותה בדיקת "לולאת דחיפות" עם fake client שמדמה בדיוק עיוותי Postgres (timestamp בפורמט `+00:00`, סדר מפתחות הפוך ב-jsonb) — `updateCallCount` נשאר יציב (1, ואז 2 אחרי מיזוג אמיתי), בלי אף לולאה. כל בדיקות ה-collab הקודמות (race/merge/polling/typing/concurrent-add) + חבילת הרגרסיה המלאה — **0 שגיאות**. `sw.js`: `CACHE_NAME` הועלה ל-`vplus-v15`. `APP_VERSION` עודכן ל-54.

**הערה חשובה**: כל הבדיקות כאן (גם בשלבים 51-53) בוצעו מול client מדומה (fake) שמדמה את התנהגות Supabase/Postgres, כי אין גישת רשת אמיתית מה-sandbox לפרויקט ה-Supabase האמיתי. זו הפעם השנייה שתיקון שנבדק כך עדיין נכשל בפועל — לכן חשוב שהמשתמש יבדוק שוב בפועל בשני מכשירים אחרי הפריסה, ואם זה עדיין לא עובד, הצעד הבא יהיה לבקש מהמשתמש לבדוק את קונסולת הדפדפן (F12) לשגיאות בפועל, כי אין יותר גישת MCP ישירה ל-Supabase לאבחון מרחוק.

## שלב 55: סנכרון מחיקת פריטים בשיתוף חי — tombstones
אחרי ששלב 54 תיקן לגמרי את סנכרון ה**הוספות**, המשתמש דיווח: **מחיקת** מוצר לא מסתנכרנת בכלל — מוצר שנמחק אצל משתמש אחד ממשיך להופיע אצל השני.

**אבחון — מגבלה ארכיטקטונית, לא עוד תקלה**: `mergeItems` (משלב 50) היא איחוד-בלבד לפי id — מתחילה מפריטי השרת ומוסיפה כל פריט שקיים **רק מקומית**. פעולה כזו יכולה **רק להוסיף**, אף פעם לא להסיר — "פריט חסר כי נמחק בכוונה" ו"פריט שעוד לא ידוע לי עליו" נראים זהים למנגנון הזה, וההתנהגות תמיד "בטוחה" (משאירה/מחזירה את הפריט). זו הסיבה שכל מחיקה נכשלה: A מוחק ודוחף רשימה בלי הפריט → B, שעדיין "מכיר" את הפריט מקומית, ממזג אותו בחזרה כ"פריט מקומי ייחודי" → ואז דוחף אותו בחזרה לשרת (אותו מנגנון ריפוי-עצמי ששומר על הוספות!) — מחזיר את הפריט גם ל-A. שני הצדדים "מגנים" זה על זה מהמחיקה של זה.

**הפתרון — tombstones**: שדה חדש `deletedIds` (מערך מזהים) על אובייקט הרשימה המשותפת, מסונכרן יחד עם `items` (אותו `data` שכבר נשלח, אין צורך בטבלה/עמודה נוספת) — "רק גדל", בדיוק כמו ש-`items` "רק מצטרפים". כל מזהה ב-`deletedIds` מוסר תמיד במיזוג, בלי קשר אם הוא עדיין מופיע מקומית או בצד השני.
- **זיהוי מחיקה מקומית**: `js/features/collab.js` שומר `lastSyncedItemIds` — הסט האחרון שכבר "טופל". בכל `pushCurrentListIfShared` (רץ על כל render): מזהה שנעלם מאז הסט האחרון → נוסף ל-`deletedIds`; מזהה שחזר (למשל "בטל" בטוסט המחיקה) → **מוסר** מ-`deletedIds`, כך שביטול מחיקה ממשיך לעבוד ומתפשט הלאה.
- `mergeItems(localItems, remoteItems, deletedIds)` — מסננת כל פריט (מהשרת או מקומי) שהמזהה שלו ב-`deletedIds` הממוזג (מקומי ∪ מרוחק), **לפני** שלב "הוסף פריטים מקומיים ייחודיים".
- תנאי ה-self-heal (`healedNewInfo`) הורחב: לא רק "נוספו פריטים", גם "נוספו תוחלות-מוות חדשות שהשרת לא הכיר" — כדי שמחיקה חדשה תתפשט גם היא, לא תישאר תקועה מקומית.
- **ללא שינוי** ב-`js/core/store.js`/`deleteItem` — המחיקה המקומית (כולל "בטל") נשארת מחיקה קשיחה בדיוק כמו היום; כל המנגנון החדש מוכל לגמרי בתוך `collab.js`, רשימות לא-משותפות לא מושפעות כלל.

נבדק ב-Playwright: (1) מחיקת פריט קיים ודחיפה → תוחלת-מוות נכונה נדחפת, הפריט נעלם. (2) עדכון מרוחק "ישן"/עצמאי שעדיין מכיל את הפריט שנמחק (לא יודע על המחיקה) → הפריט **לא** חוזר, המיזוג מכבד את תוחלת-המוות המקומית בלי קשר לתוכן המרוחק. (3) הפריט נשאר נעלם גם אחרי עוד סבב poll (לא הישרדות זמנית). (4) מחיקה מיד ואחריה "בטל" — הפריט נשאר, בלי תוחלת-מוות שגויה עבורו. כל בדיקות ה-collab הקודמות (race/merge/polling/typing/concurrent-add/roundtrip-loop) + חבילת הרגרסיה המלאה — **0 שגיאות**. `sw.js`: `CACHE_NAME` הועלה ל-`vplus-v16`. `APP_VERSION` עודכן ל-55.

## שלב 56: תיקון "בטל מחיקה לפעמים לא עובד" ברשימה משותפת
המשתמש דיווח: מוחקים פריט (טוסט עם "בטל" למשך 5 שניות), לוחצים "בטל" — לפעמים הפריט לא חוזר. קורה **רק** ברשימה משותפת, ורק **לפעמים**.

**אבחון**: ב-`js/ui/modals.js`, `showToast()` שולט בסרגל טוסט **גלובלי-יחיד** באפליקציה — כל קריאה חדשה דורסת לגמרי את הקריאה הקודמת (טקסט, מצב כפתור "בטל", טיימר), בלי לבדוק אם כרגע מוצג טוסט עם אפשרות ביטול פעילה. ב-`js/features/collab.js`, כשדחיפת רשימה משותפת נכשלת (תקלת רשת חולפת אמיתית — לא ניתנת לדימוי ב-sandbox, אבל קורית בטלפון אמיתי), נקרא `showToast('לא ניתן לעדכן...')` **בלי** אפשרות ביטול. אם זה קורה **בדיוק** בתוך חלון 5 השניות של טוסט "מחיקת פריט + בטל" — זה **דורס** אותו: מסתיר את כפתור ה"בטל" ומנקה את ה-`onclick` שלו. לחיצת המשתמש על "בטל" באותו רגע לא עושה כלום. זה מסביר בדיוק את שני הפרטים שדווחו: קורה רק ברשימה משותפת (הקריאה היחידה ל-`showToast` בלי ביטול שתלויה בפעילות רשת ברקע), ורק לפעמים (תלוי אם דווקא נפלה תקלת דחיפה בתוך חלון ה-5 שניות).

**התיקון**: `js/ui/modals.js` — משתנה `toastHasActiveUndo`; אם טוסט עם ביטול פעיל מוצג, קריאה חדשה **בלי** ביטול מתעלמת ולא דורסת. `hideToast()` מאפס את הדגל. **תופעת לוואי מוצהרת ומקובלת**: הודעת שגיאת הרשת עלולה "להיבלע" פעם אחת נדירה אם היא נופלת בדיוק בחלון ביטול-מחיקה — מקובל כי זו רק הודעת מידע על ניסיון שיחזור על עצמו אוטומטית (polling), ושמירת יכולת ה"בטל" חשובה יותר.

נבדק ב-Playwright: טוסט מחיקה+ביטול מוצג, ותוך כדי כך מגיעה קריאת `showToast` בלי ביטול (מדמה שגיאת רשת) — כפתור "בטל" נשאר גלוי ופעיל, לחיצה עליו עדיין משחזרת את הפריט. אומת גם שאחרי שהטוסט עם הביטול נסגר, טוסט רגיל הבא כן מוצג כרגיל (לא נחסם לצמיתות). כל בדיקות ה-collab הקודמות + חבילת הרגרסיה המלאה — **0 שגיאות**. `sw.js`: `CACHE_NAME` הועלה ל-`vplus-v17`. `APP_VERSION` עודכן ל-56.

## שלב 57: עיצוב מחדש לחלונית הוספת פריט — פשטות (שם + כמות בלבד) לפי בקשת המשתמש
המשתמש ביקש עיצוב מחדש לחלונית הוספת מוצר: כברירת מחדל יופיעו רק שם הפריט וכמות, שאר האפשרויות (מחיר, קטגוריה, תאריך יעד, הערה, קישור תשלום, הוספה רציפה) מאחורי כפתור הרחבה — והחלונית חייבת להתחיל מהחלק העליון של המסך כדי שיישאר רווח בין המקלדת (שתופסת כ-38% מהמסך באנדרואיד) לחלונית.

**תהליך**: הוצג לוח עיצוב (`/design`) עם 5 קונספטים שונים כפרוטוטיפ עובד (Artifact נפרד) — כולם top-anchored, כולם עם כפתור בדיקת-מרווח-למקלדת מודמה. המשתמש בחר **אפשרות 2 — "שורה אחת קומפקטית"**: שם הפריט וכמות (כ-Stepper עם +/-) באותה שורה, כפתור "עוד אפשרויות" מתחתיה.

**מימוש בפועל (`index.html`, `style.css`, `js/features/item-crud.js`)**:
- `#itemModal`: `itemName` ו-`itemQty` אוחדו לשורה אחת (`.quick-name-qty-row`) — שם הפריט בעטיפה חדשה `.quick-name-wrap` (`position:relative`, לשמירה על מיקום ה-autocomplete), `itemQty` הפך ל-Stepper אמיתי (`.qty-stepper` עם כפתורי `itemQtyMinus`/`itemQtyPlus`), `voiceBtn` נשאר בסוף השורה.
- `itemPrice` הועבר **לתוך** `#itemAdvancedFields` (לצד `itemCategorySelect` בשורת `.form-row`); `continuousToggleWrap` (⚡ הוספה רציפה) הועבר גם הוא לתוך `#itemAdvancedFields` (במקום בכותרת) — כל ה-id-ים הקיימים נשמרו במדויק, אין שינוי בלוגיקת השמירה/עריכה ב-`item-crud.js`.
- כפתור ההרחבה (`itemAdvancedToggle`) עוצב כצ'יפ סגול עם שברון מסתובב (`itemAdvancedChevron`) וטקסט דינמי (`itemAdvancedToggleLabel`: "עוד אפשרויות" / "הצג פחות") דרך פונקציה חדשה `setAdvancedFieldsOpen(open)`.
- **החלטת UX חדשה**: בפתיחת **עריכת** פריט קיים, השדות המורחבים נפתחים **אוטומטית** (`setAdvancedFieldsOpen(true)`) — כי בעריכה סביר שהמשתמש רוצה לראות/לשנות מחיר/קטגוריה מיד, בלי קליק נוסף. בהוספת פריט **חדש** הם עדיין מתחילים סגורים, כמבוקש.
- `.quick-add-header` (עטיפת הכותרת+המתג הישנה) הוסרה — כבר לא בשימוש; `.quick-form-row` (dead CSS) הוסרה.
- מודל ה-`.modal-backdrop` כבר היה `align-items:flex-start` (top-anchored) מלכתחילה — לא נדרש שינוי שם.

נבדק ב-Playwright + צילומי מסך: כפתורי +/- משנים כמות (לא יורד מתחת ל-1), הרחבה/כיווץ עובדים ומעדכנים תווית, זרימת הוספה מלאה עדיין עובדת (כולל מחיר מתוך השדות המורחבים), זרימת עריכה נפתחת עם השדות המורחבים גלויים ומאוכלסים נכון. אומת גם (בהשוואה ישירה ל-git stash של הקוד המקורי) שהתנהגות "autocomplete נעלם אחרי הקלדה אוטומטית ב-Playwright" היא **תקלת סימולציה קיימת מראש בקוד המקורי** (לא regression מהשינוי הזה) — קשורה לאופן שבו `page.type()` מדמה הקלדה, לא רלוונטית למשתמש אמיתי במקלדת אמיתית. כל בדיקות ה-collab (כולל אינדיקטור הקלדה שתלוי ב-`#itemName`) + חבילת הרגרסיה המלאה — **0 שגיאות**. `sw.js`: `CACHE_NAME` הועלה ל-`vplus-v18`. `APP_VERSION` עודכן ל-57.

## Current Focus
**הבנייה המלאה + מחזור עיצובים + שיתוף רשימה חי (Supabase) + תיקוני סנכרון (חד-כיווני, פריט נעלם זמנית, מיזוג מקבילי, אובדן קבוע בכתיבה מקבילה, שיטת סנכרון פושטה לגמרי, סנכרון מחיקות עם tombstones, הגנה על טוסט-ביטול מדריסה) + שמירת Supabase ער (GitHub Actions) + מספר גרסה בהדר + אינדיקטור "מישהו מזין פריט" (Broadcast) + עיצוב מחדש לחלונית הוספת פריט (שם+כמות בלבד, שאר האפשרויות מאחורי הרחבה) הושלמו ומוזגו ל-main.**

**אושר ע"י המשתמש (גרסה 55, בדיקה אמיתית בשני מכשירים)**: השיתוף החי עובד מצוין — הוספת פריטים ומחיקת פריטים מסתנכרנות נכון בין 2 משתמשים. זה **התיקון הנכון** לבעיית הסנכרון (אחרי כמה ניסיונות קודמים שנכשלו בפועל למרות שעברו בדיקות — שלבים 51-54). אם תתגלה בעיה נוספת בסנכרון, זה נקודת ההתייחסות: השיטה מ-שלב 54 (מיזוג-איחוד עם `suppressNextPush` לפי השוואת מספרים, לא טקסט) + tombstones משלב 55 היא הבסיס היציב שאושר בפועל — להימנע מלחזור לגישות השוואת timestamp/JSON כמחרוזות (שלבים 52-53, שניהם נכשלו בפועל).

נותר כפתרון המשך: קוד QR לשיתוף פנים-אל-פנים. האפליקציה כוללת: רשימות/פריטים מלאים, קטגוריות אוטומטיות וצבועות, היסטוריה וסטטיסטיקה עם גרפים, Autocomplete/מחירון, ייבוא (טקסט/לוח/Excel/קול), גרירה-לסידור, נעילה, בחירה מרובה, הדפסה, PWA מלא, נגישות טקסט (הגדלת אותיות), ועיצוב בהיר/צבעוני התואם למקור (סגול/ירוק, סרגל גרדיאנט תחתון, פילים צבעוניים) — הכל ללא Firebase, ללא מודול פיננסי, ללא הגדרות/פוש/גוגל לוגין/סריקת קבלה, לפי ההחלטות שנקבעו.

**פערים ידועים מול shopping-list המקורי** (נשמטו במודע, ניתן להוסיף אם יתבקש): מצב קומפקטי (גובה כרטיס), חיוב חודשי קבוע + שורת "נדחה לחודש הבא", ניהול קטגוריות מותאמות אישית (היה בתוך ההגדרות המוחרגות).

ממתינים להנחיית המשתמש לצעד הבא (תיקונים/פיצ'רים נוספים/בדיקה ידנית).

## משימות
- [x] אימות מצב ריפו vplus (ריק, ברנץ' מעודכן)
- [x] איתור וצירוף ריפו shopping-list כמקור השראה
- [x] יצירת PROJECT_STATE.md
- [x] יצירת שלד פרויקט — מבנה תיקיות בסיסי
- [x] הסרת תלות ב-Firebase מהשלד (החלטה: ללא שרת בשלב זה)
- [x] תיעוד החלטת היקף: ללא מודול פיננסי/חיבור לבנק
- [x] תיעוד החלטת היקף: ללא שורת כפתורי מיון/תרגום/שיתוף/סיווג מחדש/מדריך/תבנית
- [x] תיעוד החלטת היקף: ללא כפתור הגדרות ותוכנו
- [x] תיעוד החלטת היקף: ללא התראות Push
- [x] תיעוד החלטת היקף: ללא התחברות עם חשבון Google
- [x] קבלת הנחיית המשתמש לצעד הבא בבנייה — בוצעה בנייה מלאה (10 שלבים) + עיצוב מחדש Nocturne (שלב 11)
- [ ] קבלת הנחיית המשתמש לצעד הבא
